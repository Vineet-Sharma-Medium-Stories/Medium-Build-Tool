
# Complete Gemini Image Generation Prompts

## Node.js Express Containerization Series - All 22 Stories (Azure + AWS)

## Main prompt
```
Great Now create the the gemini image generation prompt for:
1."Publishing Node.js Express Apps as Container Images: A Complete Guide to 10 Deployment Approaches" story with appropriate style and logs with connectors (extract from story) with sub-title. Place "Vineet Sharma" at bottom Right
2. "Publishing Node.js Express Apps as Container Images: A Complete Guide to 10 Deployment Approaches - AWS" story with appropriate style and logs with connectors (extract from story) with sub-title. Place "Vineet Sharma" at bottom Right
3. For individual concepts/stories for Azure sub-title
4. For individual concepts/stories for AWS sub-title
```
---

### Main Series Banners (2 Prompts)

---

#### Prompt 1: Main Series Banner - Node.js Express Containerization Guide

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for "Publishing Node.js Express Apps as Container Images: A Complete Guide to 10 Deployment Approaches - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Node.js logo pattern in the background
- Subtle Azure logo pattern overlay (very light, almost transparent)

**Central Elements:**
- Prominent Node.js logo (green) with "Express.js" badge on left side
- Azure logo (blue) prominently displayed on right side
- Ten radial connectors emanating from the Node.js logo, each leading to an icon representing one of the 10 Azure deployment approaches
- The connectors should form a circular or semi-circular pattern around the Node.js logo

**10 Deployment Approach Icons:**
1. **NPM + Docker Multi-Stage** - npm icon with container
2. **Yarn + Docker** - Yarn icon with container
3. **pnpm + Docker** - pnpm icon with container
4. **Azure Container Apps** - Azure container icon
5. **VS Code Dev Containers** - VS Code icon with Azure extension
6. **Azure Developer CLI (azd)** - Terminal with Azure logo
7. **Tarball Export** - Archive/package icon with shield
8. **Azure Kubernetes Service (AKS)** - AKS logo
9. **GitHub Actions + ACR** - GitHub logo with ACR icon
10. **AWS CDK & Copilot (Multi-Cloud)** - AWS CDK and Copilot icons with Azure integration

**Azure Technology Stack Row (bottom):**
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Kubernetes Service (AKS icon)
- Azure Developer CLI (azd logo)
- Bicep (infrastructure code icon)
- Azure Monitor (monitoring icon)
- Application Insights (chart icon)
- Microsoft Defender for Cloud (shield icon)

**Text Elements:**
- Main title: "Publishing Node.js Express Apps as Container Images" in bold sans-serif font
- Subtitle: "A Complete Guide to 10 Deployment Approaches - Azure Edition" below main title
- "Azure Cloud" label near Azure logo
- "Scale to Zero" badge near the ACA icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 2: Main Series Banner - Node.js Express Containerization Guide - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for "Publishing Node.js Express Apps as Container Images: A Complete Guide to 10 Deployment Approaches - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Node.js logo pattern in the background
- Subtle AWS logo pattern overlay (very light, almost transparent)

**Central Elements:**
- Prominent Node.js logo (green) with "Express.js" badge on left side
- AWS logo (orange) prominently displayed on right side
- Ten radial connectors emanating from the Node.js logo, each leading to an icon representing one of the 10 AWS deployment approaches
- The connectors should form a circular or semi-circular pattern around the Node.js logo

**10 Deployment Approach Icons:**
1. **NPM + Docker Multi-Stage** - npm icon with container
2. **Yarn + Docker** - Yarn icon with container
3. **pnpm + Docker** - pnpm icon with container
4. **AWS Copilot** - Copilot icon
5. **VS Code Dev Containers** - VS Code icon with AWS Toolkit
6. **AWS CDK with TypeScript** - AWS CDK logo
7. **Tarball Export** - Archive/package icon with shield
8. **Amazon EKS** - EKS logo
9. **GitHub Actions + ECR** - GitHub logo with ECR icon
10. **AWS App Runner** - App Runner icon

**AWS Technology Stack Row (bottom):**
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- AWS Graviton (chip icon)
- AWS Copilot (copilot icon)
- AWS CDK (CDK icon)
- AWS App Runner (App Runner icon)
- Amazon Inspector (shield icon)

**Text Elements:**
- Main title: "Publishing Node.js Express Apps as Container Images" in bold sans-serif font
- Subtitle: "A Complete Guide to 10 Deployment Approaches - AWS Edition" below main title
- "AWS Cloud" label near AWS logo
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Azure Edition (10 Stories)

---

#### Prompt 3: NPM + Docker Multi-Stage - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "NPM + Docker Multi-Stage: The Classic Node.js Approach - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle npm logo pattern in the background

**Central Elements:**
- Prominent npm icon (red/brown) on left side
- Azure logo (blue) on right side
- Dockerfile document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (npm ci)
  - Install dependencies layer
  - Runtime stage (Node.js Alpine)
  - Final image

**Key Technology Icons:**
- npm (npm icon)
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Node.js (Node.js icon)
- Express.js (lightning bolt with Express)

**Text Elements:**
- Main title: "NPM + Docker Multi-Stage" in bold sans-serif font
- Subtitle: "The Classic Node.js Approach - Azure Edition" below main title
- "Multi-Stage Builds" badge near the layer visualization
- "npm ci" badge for deterministic builds

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 4: Yarn + Docker - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Yarn + Docker: Deterministic Dependency Management - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Yarn logo pattern in the background

**Central Elements:**
- Prominent Yarn icon (blue) on left side
- Azure logo (blue) on right side
- Yarn Berry (Yarn 2+) lock file icon in the center
- Visual representation of Yarn Berry features:
  - Plug'n'Play (PnP) architecture (puzzle piece icon)
  - Zero-install (checkmark with cache icon)
  - .yarn/cache directory visualization
  - .pnp.cjs file icon

**Key Technology Icons:**
- Yarn (yarn icon)
- Yarn Berry (yarn icon with 2+ badge)
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Plug'n'Play (puzzle piece icon)
- Zero-install (cache icon)

**Text Elements:**
- Main title: "Yarn + Docker" in bold sans-serif font
- Subtitle: "Deterministic Dependency Management - Azure Edition" below main title
- "Plug'n'Play (PnP)" badge near the puzzle piece icon
- "Zero-Install" badge near the cache icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 5: pnpm + Docker - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "pnpm + Docker: Disk-Efficient Node.js Containers - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle pnpm logo pattern in the background

**Central Elements:**
- Prominent pnpm icon (orange) on left side
- Azure logo (blue) on right side
- Visual representation of pnpm's content-addressable storage:
  - Global store (.pnpm-store) icon
  - Symlink connections to multiple projects
  - Flat node_modules structure with symlinks

**Key Technology Icons:**
- pnpm (pnpm icon)
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Content-addressable store (database with chain icon)
- Symlink (link icon)

**Text Elements:**
- Main title: "pnpm + Docker" in bold sans-serif font
- Subtitle: "Disk-Efficient Node.js Containers - Azure Edition" below main title
- "30-50% Smaller Images" badge near the storage visualization
- "Content-Addressable Storage" badge

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 6: Azure Container Apps - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Container Apps: Serverless Node.js Deployment" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Azure Container Apps logo pattern in the background

**Central Elements:**
- Prominent Azure Container Apps (ACA) logo at the center
- Node.js logo (green) with "Express.js" badge connected to ACA
- Visual representation of ACA's serverless architecture:
  - Auto-scaling rules (scale up/down arrows)
  - Revision management (version icons)
  - Ingress configuration (traffic flow)
  - Scale to zero (zero icon)

**Key Technology Icons:**
- Azure Container Apps (ACA logo)
- Azure Container Registry (ACR icon)
- Azure Monitor (monitoring icon)
- KEDA (KEDA icon)
- Dapr (Dapr icon)

**Text Elements:**
- Main title: "Azure Container Apps" in bold sans-serif font
- Subtitle: "Serverless Node.js Deployment" below main title
- "Scale to Zero" badge near the zero icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 7: Visual Studio Code Dev Containers - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio Code Dev Containers: Local Development to Production - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle VS Code logo pattern in the background

**Central Elements:**
- Prominent VS Code logo (blue) on left side
- Azure logo (blue) on right side
- Dev container visualization in the center:
  - Container icon with VS Code inside
  - Environment variables configuration
  - Port forwarding arrows
- LocalStack emulator icon for Azure service emulation

**Key Technology Icons:**
- VS Code (VS Code logo)
- Dev Containers (container with code icon)
- Docker (whale icon)
- Azure CLI (terminal icon)
- Azure Storage Emulator (storage icon)
- Azure Functions Core Tools (function icon)

**Text Elements:**
- Main title: "Visual Studio Code Dev Containers" in bold sans-serif font
- Subtitle: "Local Development to Production - Azure Edition" below main title
- "Consistent Environments" badge near the dev container

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 8: Azure Developer CLI (azd) - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Developer CLI (azd) with Node.js: The Turnkey Solution - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Azure logo pattern in the background

**Central Elements:**
- Azure Developer CLI (azd) logo (terminal with Azure symbol) prominently displayed
- Node.js logo (green) with "Express.js" badge connected to the azd logo
- Flow visualization showing the three phases of `azd up`:
  1. Package (container icon)
  2. Provision (infrastructure icon)
  3. Deploy (cloud with arrow)
- Auto-provisioned Azure resources radiating from the flow:
  - Azure Container Registry (ACR)
  - Azure Container Apps (ACA)
  - Azure Cosmos DB (database)
  - Azure Redis Cache (cache)
  - Application Insights (monitoring)

**Key Technology Icons:**
- Azure Developer CLI (azd logo)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Cosmos DB (database icon)
- Bicep (infrastructure code icon)

**Text Elements:**
- Main title: "Azure Developer CLI (azd) with Node.js" in bold sans-serif font
- Subtitle: "The Turnkey Solution - Azure Edition" below main title
- "One Command: azd up" badge near the central flow

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 9: Tarball Export - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (blue)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to ACR (cloud icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Microsoft Defender for Cloud (Defender logo)
- Azure Key Vault (key icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - Azure Edition" below main title
- "Air-Gapped Support" badge near the approval gate

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 10: Azure Kubernetes Service (AKS) - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Kubernetes Service (AKS): Node.js Microservices at Scale" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Azure Kubernetes Service (AKS) logo connected to the helm
- Visual representation of AKS cluster architecture:
  - Control plane (API Server, etcd, Scheduler)
  - Worker nodes (multiple node icons)
  - Pods running Node.js containers
  - Services and Ingress

**Key Technology Icons:**
- Kubernetes (helm icon)
- Azure Kubernetes Service (AKS logo)
- Azure Container Registry (ACR icon)
- Helm (helm icon)
- Horizontal Pod Autoscaler (scaling icon)
- Azure Monitor (monitoring icon)

**Text Elements:**
- Main title: "Azure Kubernetes Service (AKS)" in bold sans-serif font
- Subtitle: "Node.js Microservices at Scale" below main title
- "Azure Kubernetes Service" badge near the AKS logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 11: GitHub Actions + Container Registry - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "GitHub Actions + Container Registry: CI/CD for Node.js - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle GitHub logo pattern in the background

**Central Elements:**
- Prominent GitHub logo (dark) on left side
- Azure logo (blue) on right side
- Azure Container Registry (ACR) icon in the center
- CI/CD workflow visualization:
  - Code push (Git commit icon)
  - GitHub Actions workflow (gear icon)
  - Docker build (container icon)
  - Trivy security scan (shield icon)
  - Push to ACR (upload icon)
  - Deploy to ACA (deploy icon)

**Key Technology Icons:**
- GitHub (GitHub logo)
- GitHub Actions (workflow icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Trivy (trivy logo)
- OIDC (identity icon)

**Text Elements:**
- Main title: "GitHub Actions + Container Registry" in bold sans-serif font
- Subtitle: "CI/CD for Node.js - Azure Edition" below main title
- "OIDC Authentication" badge near the identity icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 12: AWS CDK & Copilot - Multi-Cloud - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS CDK & Copilot: Multi-Cloud Node.js Container Deployments" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle AWS and Azure logo pattern in the background

**Central Elements:**
- AWS CDK logo (blue) on left side
- AWS Copilot icon (copilot) on right side
- Node.js logo (green) with "Express.js" badge at the center
- Flowing connector lines between AWS CDK, Copilot, and Node.js
- Azure logo integrated to show multi-cloud capability
- Visual representation of multi-cloud deployment:
  - TypeScript code snippet on one side
  - CloudFormation stack on the other side
  - ECS Fargate service with Azure Container Apps alternative

**Key Technology Icons:**
- AWS CDK (CDK logo)
- AWS Copilot (copilot icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Azure logo (Azure icon)
- TypeScript (TypeScript logo)

**Text Elements:**
- Main title: "AWS CDK & Copilot" in bold sans-serif font
- Subtitle: "Multi-Cloud Node.js Container Deployments" below main title
- "Azure + AWS" badge near the center

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### AWS Edition (10 Stories)

---

#### Prompt 13: NPM + Docker Multi-Stage - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "NPM + Docker Multi-Stage: The Classic Node.js Approach - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle npm logo pattern in the background

**Central Elements:**
- Prominent npm icon (red/brown) on left side
- AWS logo (orange) on right side
- Dockerfile document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (npm ci)
  - Install dependencies layer
  - Runtime stage (Node.js Alpine)
  - Final image
- AWS Graviton chip icon integrated into the architecture

**Key Technology Icons:**
- npm (npm icon)
- Docker (whale icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- Node.js (Node.js icon)

**Text Elements:**
- Main title: "NPM + Docker Multi-Stage" in bold sans-serif font
- Subtitle: "The Classic Node.js Approach - AWS" below main title
- "Multi-Stage Builds" badge near the layer visualization
- "Graviton Ready" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 14: Yarn + Docker - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Yarn + Docker: Deterministic Dependency Management - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Yarn logo pattern in the background

**Central Elements:**
- Prominent Yarn icon (blue) on left side
- AWS logo (orange) on right side
- Yarn Berry (Yarn 2+) lock file icon in the center
- Visual representation of Yarn Berry features:
  - Plug'n'Play (PnP) architecture (puzzle piece icon)
  - Zero-install (checkmark with cache icon)
  - .yarn/cache directory visualization
  - .pnp.cjs file icon

**Key Technology Icons:**
- Yarn (yarn icon)
- Yarn Berry (yarn icon with 2+ badge)
- Docker (whale icon)
- Amazon ECR (ECR icon)
- AWS CodeBuild (CodeBuild icon)
- Plug'n'Play (puzzle piece icon)

**Text Elements:**
- Main title: "Yarn + Docker" in bold sans-serif font
- Subtitle: "Deterministic Dependency Management - AWS" below main title
- "Zero-Install in CodeBuild" badge near the cache icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 15: pnpm + Docker - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "pnpm + Docker: Disk-Efficient Node.js Containers - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle pnpm logo pattern in the background

**Central Elements:**
- Prominent pnpm icon (orange) on left side
- AWS logo (orange) on right side
- Visual representation of pnpm's content-addressable storage:
  - Global store (.pnpm-store) icon
  - Symlink connections to multiple projects
  - Flat node_modules structure with symlinks
- AWS Graviton chip icon integrated for optimization

**Key Technology Icons:**
- pnpm (pnpm icon)
- Docker (whale icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- Content-addressable store (database with chain icon)

**Text Elements:**
- Main title: "pnpm + Docker" in bold sans-serif font
- Subtitle: "Disk-Efficient Node.js Containers - AWS" below main title
- "30-50% Smaller ECR Images" badge near the storage visualization
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 16: AWS Copilot - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS Copilot: The Turnkey Container Solution - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS Copilot logo pattern in the background

**Central Elements:**
- Prominent AWS Copilot logo (copilot icon) at the center
- Node.js logo (green) with "Express.js" badge connected to Copilot
- Flow visualization showing Copilot workflow:
  - `copilot init` (initialization icon)
  - `copilot env init` (environment icon)
  - `copilot deploy` (deploy icon)
- Auto-provisioned AWS resources radiating from the flow:
  - Amazon ECR (ECR icon)
  - Amazon ECS with Fargate (ECS icon)
  - Amazon DocumentDB (database icon)
  - ElastiCache Redis (cache icon)
  - Application Load Balancer (ALB icon)

**Key Technology Icons:**
- AWS Copilot (copilot icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- AWS Secrets Manager (key icon)

**Text Elements:**
- Main title: "AWS Copilot" in bold sans-serif font
- Subtitle: "The Turnkey Container Solution - AWS" below main title
- "Graviton Optimized" badge near the ECS icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 17: Visual Studio Code Dev Containers - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio Code Dev Containers: Local Development to Production - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle VS Code logo pattern in the background

**Central Elements:**
- Prominent VS Code logo (blue) on left side
- AWS logo (orange) on right side
- Dev container visualization in the center:
  - Container icon with VS Code inside
  - Environment variables configuration
  - Port forwarding arrows
- LocalStack emulator icon for AWS service emulation (Secrets Manager, S3, SQS)

**Key Technology Icons:**
- VS Code (VS Code logo)
- Dev Containers (container with code icon)
- Docker (whale icon)
- AWS CLI (terminal icon)
- LocalStack (stack icon)
- AWS Toolkit (AWS logo with VS)

**Text Elements:**
- Main title: "Visual Studio Code Dev Containers" in bold sans-serif font
- Subtitle: "Local Development to Production - AWS" below main title
- "LocalStack Integration" badge near the stack icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 18: AWS CDK with TypeScript - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS CDK with TypeScript: Infrastructure as Code for Containers - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS CDK logo pattern in the background

**Central Elements:**
- Prominent AWS CDK logo (blue) on left side
- TypeScript logo (blue) on right side
- Node.js logo (green) with "Express.js" badge at the center
- Flowing connector lines between CDK, TypeScript, and Node.js
- Visual representation of infrastructure as code:
  - TypeScript code snippet on one side
  - CloudFormation stack on the other side
  - ECS Fargate service in the middle

**Key Technology Icons:**
- AWS CDK (CDK logo)
- TypeScript (TypeScript logo)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Application Load Balancer (ALB icon)
- AWS CloudFormation (stack icon)

**Text Elements:**
- Main title: "AWS CDK with TypeScript" in bold sans-serif font
- Subtitle: "Infrastructure as Code for Containers - AWS" below main title
- "TypeScript CDK" badge near the TypeScript logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 19: Tarball Export - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (orange)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to Amazon ECR (ECR icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Amazon Inspector (Inspector logo)
- AWS Signer (signature icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - AWS" below main title
- "Air-Gapped Support" badge near the approval gate

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 20: Amazon EKS - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Amazon EKS: Node.js Microservices at Scale - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Amazon EKS logo connected to the helm
- Visual representation of EKS cluster architecture:
  - Control plane (AWS managed)
  - Managed node groups (multiple node icons with Graviton badge)
  - Pods running Node.js containers
  - ALB Ingress Controller (ALB icon)

**Key Technology Icons:**
- Kubernetes (helm icon)
- Amazon EKS (EKS logo)
- Amazon ECR (ECR icon)
- AWS Load Balancer Controller (ALB icon)
- AWS Graviton (chip icon)
- Helm (helm icon)

**Text Elements:**
- Main title: "Amazon EKS" in bold sans-serif font
- Subtitle: "Node.js Microservices at Scale - AWS" below main title
- "Amazon EKS" badge near the EKS logo
- "Graviton Node Groups" badge near the node icons

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 21: GitHub Actions + Amazon ECR - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "GitHub Actions + Amazon ECR: CI/CD for Node.js - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle GitHub logo pattern in the background

**Central Elements:**
- Prominent GitHub logo (dark) on left side
- AWS logo (orange) on right side
- Amazon ECR icon in the center
- CI/CD workflow visualization:
  - Code push (Git commit icon)
  - GitHub Actions workflow (gear icon)
  - Docker build (container icon)
  - Trivy security scan (shield icon)
  - Push to ECR (upload icon)
  - Deploy to ECS (deploy icon)

**Key Technology Icons:**
- GitHub (GitHub logo)
- GitHub Actions (workflow icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Trivy (trivy logo)
- OIDC (identity icon)

**Text Elements:**
- Main title: "GitHub Actions + Amazon ECR" in bold sans-serif font
- Subtitle: "CI/CD for Node.js - AWS" below main title
- "OIDC Authentication" badge near the identity icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 22: AWS App Runner - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS App Runner: Fully Managed Node.js Container Service - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS App Runner logo pattern in the background

**Central Elements:**
- Prominent AWS App Runner logo (App Runner icon) at the center
- Node.js logo (green) with "Express.js" badge connected to App Runner
- Visual representation of App Runner's fully managed architecture:
  - Source (GitHub/ECR) icon
  - Automatic Build icon
  - Container Deployment icon
  - Auto Scaling icon
  - Built-in Load Balancer icon
  - Automatic SSL/TLS icon

**Key Technology Icons:**
- AWS App Runner (App Runner icon)
- Amazon ECR (ECR icon)
- GitHub (GitHub logo)
- CloudWatch (monitoring icon)
- AWS Secrets Manager (key icon)
- Auto Scaling (scaling icon)

**Text Elements:**
- Main title: "AWS App Runner" in bold sans-serif font
- Subtitle: "Fully Managed Node.js Container Service - AWS" below main title
- "Zero Infrastructure Management" badge near the center

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

This completes all **22 prompts** for the Node.js Express Containerization series:
- **2 Main Series Banners** (1 Azure/General + 1 AWS Edition)
- **10 Azure Edition Stories**
- **10 AWS Edition Stories**

Each prompt includes:
- Unique subtitle and styling for each story
- Appropriate icons, connectors, and technology stack elements
- Cloud-specific accents (Azure blue for Azure editions, AWS orange for AWS editions)
- Attribution **"Vineet Sharma" in bold font at the bottom right corner**