# Main prompt
```
Great Now create the the gemini image generation prompt for:
1. .Net "Publishing .NET 10 Apps as Container Images: A Complete Guide to 9 Deployment Approaches" story with appropriate style and logs with connectors (extract from story) with sub-title. Place "Vineet Sharma" at bottom Right
2. For individual concepts/stories for Azure sub-title
3. For individual concepts/stories for AWS sub-title
```

# Series: Publishing .NET 10 Apps as Container Images - A Complete Guide to 9 Deployment Approaches

---

### Prompt 1: Main Series Banner - .NET 10 Containerization Guide

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for "Publishing .NET 10 Apps as Container Images: A Complete Guide to 9 Deployment Approaches" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light purple (#e9d8fd)
- Subtle .NET logo watermark pattern in the background

**Central Elements:**
- Prominent .NET logo (purple/magenta) with "10" badge at the center
- Nine radial connectors emanating from the .NET logo, each leading to an icon representing one of the 9 deployment approaches
- The connectors should form a circular or semi-circular pattern around the logo

**9 Deployment Approach Icons:**
1. **SDK Native Container Publishing** - Cube icon with .NET symbol
2. **Dockerfile with Docker** - Whale icon (Docker)
3. **Dockerfile with Podman** - Leaf icon (Podman)
4. **Azure Developer CLI (azd)** - Azure logo with terminal
5. **Visual Studio GUI** - Visual Studio logo
6. **Tarball Export** - Archive/package icon with shield
7. **Podman + SDK Native** - Merge icon (two arrows combining)
8. **konet** - Tool/gear icon with rocket
9. **Kubernetes** - Helm icon (steering wheel)

**Technology Stack Row (bottom):**
- MSBuild (gear icon)
- Native AOT (lightning bolt icon)
- OCI Builder (container layers icon)
- Graviton (chip icon)
- Azure Container Registry (ACR icon)
- Amazon ECR (ECR icon)
- Azure Kubernetes Service (AKS icon)
- Amazon EKS (EKS icon)

**Text Elements:**
- Main title: "Publishing .NET 10 Apps as Container Images" in bold sans-serif font
- Subtitle: "A Complete Guide to 9 Deployment Approaches" below main title
- "Azure" and "AWS" labels near respective cloud service icons

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos. Use a subtle, elegant font with slight opacity or a minimal badge style to ensure visibility without distracting from the main design.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle gradients
- 3D elements with soft shadows
- Flat design with slight depth
- High resolution (1920x1080 or larger)
```

---

### Prompt 2: .NET SDK Native Container Publishing - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story ".NET SDK Native Container Publishing: Building OCI Images Without Docker - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle .NET logo pattern in the background

**Central Elements:**
- .NET logo (purple/magenta) with "10" badge on left side
- Azure logo (blue) prominently displayed on right side
- Flowing connector lines between .NET and Azure logos
- Visual representation of OCI image layers stacked in the middle:
  - Base OS layer
  - .NET Runtime layer
  - Application dependencies layer
  - Application code layer

**Key Technology Icons:**
- MSBuild (gear icon)
- Native AOT (lightning bolt icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Kubernetes Service (AKS icon)
- Azure Developer CLI (terminal icon)

**Text Elements:**
- Main title: ".NET SDK Native Container Publishing" in bold sans-serif font
- Subtitle: "Building OCI Images Without Docker - Azure Edition" below main title
- "Azure Cloud" text near Azure logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 3: .NET SDK Native Container Publishing - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story ".NET SDK Native Container Publishing: Building OCI Images Without Docker - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle .NET logo pattern in the background

**Central Elements:**
- .NET logo (purple/magenta) with "10" badge on left side
- AWS logo (orange) prominently displayed on right side
- Flowing connector lines between .NET and AWS logos
- Visual representation of OCI image layers stacked in the middle:
  - Base OS layer
  - .NET Runtime layer
  - Application dependencies layer
  - Application code layer
- AWS Graviton chip icon integrated into the architecture

**Key Technology Icons:**
- MSBuild (gear icon)
- Native AOT (lightning bolt icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- AWS Copilot (copilot icon)
- konet (tool icon)

**Text Elements:**
- Main title: ".NET SDK Native Container Publishing" in bold sans-serif font
- Subtitle: "Building OCI Images Without Docker - AWS Edition" below main title
- "AWS Cloud" text near AWS logo
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 4: Traditional Dockerfile with Docker - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Traditional Dockerfile with Docker: The Classic Approach - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Docker whale silhouette pattern in the background

**Central Elements:**
- Prominent Docker whale icon (blue) on left side
- Azure logo (blue) on right side
- Dockerfile document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (SDK)
  - Publish stage
  - Runtime stage (ASP.NET)
  - Final image

**Key Technology Icons:**
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Kubernetes Service (AKS icon)
- .NET SDK (cube icon)
- .NET Runtime (runtime icon)

**Text Elements:**
- Main title: "Traditional Dockerfile with Docker" in bold sans-serif font
- Subtitle: "The Classic Approach - Azure Edition" below main title
- "Multi-Stage Builds" badge near the layer visualization

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Docker blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 5: Traditional Dockerfile with Docker - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Traditional Dockerfile with Docker: The Classic Approach - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Docker whale silhouette pattern in the background

**Central Elements:**
- Prominent Docker whale icon (blue) on left side
- AWS logo (orange) on right side
- Dockerfile document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (SDK)
  - Publish stage
  - Runtime stage (ASP.NET)
  - Final image

**Key Technology Icons:**
- Docker (whale icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- .NET SDK (cube icon)
- .NET Runtime (runtime icon)
- AWS Copilot (copilot icon)

**Text Elements:**
- Main title: "Traditional Dockerfile with Docker" in bold sans-serif font
- Subtitle: "The Classic Approach - AWS Edition" below main title
- "Multi-Stage Builds" badge near the layer visualization

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 6: Traditional Dockerfile with Podman - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Traditional Dockerfile with Podman: The Daemonless Alternative - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle leaf/plant pattern representing Podman's rootless nature

**Central Elements:**
- Prominent Podman leaf icon (green) on left side
- Azure logo (blue) on right side
- Visual representation of rootless container architecture:
  - User namespace
  - Container process (rootless)
  - No central daemon
- Flowing connector lines between Podman and Azure logos

**Key Technology Icons:**
- Podman (leaf icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Kubernetes Service (AKS icon)
- Rootless badge (shield with leaf)
- Daemonless badge (no daemon icon)

**Text Elements:**
- Main title: "Traditional Dockerfile with Podman" in bold sans-serif font
- Subtitle: "The Daemonless Alternative - Azure Edition" below main title
- "Rootless Security" badge near the architecture visualization

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle green accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 7: Traditional Dockerfile with Podman - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Traditional Dockerfile with Podman: The Daemonless Alternative - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle leaf/plant pattern representing Podman's rootless nature

**Central Elements:**
- Prominent Podman leaf icon (green) on left side
- AWS logo (orange) on right side
- Visual representation of rootless container architecture:
  - User namespace
  - Container process (rootless)
  - No central daemon
- Flowing connector lines between Podman and AWS logos
- EC2 instance profile integration visualization

**Key Technology Icons:**
- Podman (leaf icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- EC2 Instance Profile (EC2 icon)
- Rootless badge (shield with leaf)

**Text Elements:**
- Main title: "Traditional Dockerfile with Podman" in bold sans-serif font
- Subtitle: "The Daemonless Alternative - AWS Edition" below main title
- "Rootless Security on EC2" badge near the architecture visualization

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents and green accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 8: Azure Developer CLI (azd) with .NET Aspire - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Developer CLI (azd) with .NET Aspire: The Turnkey Solution - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Azure logo pattern in the background

**Central Elements:**
- Azure Developer CLI (azd) logo (terminal with Azure symbol) prominently displayed
- .NET Aspire logo (aspire symbol) connected to the azd logo
- Flow visualization showing the three phases of `azd up`:
  1. Package (container icon)
  2. Provision (infrastructure icon)
  3. Deploy (cloud with arrow)
- Auto-provisioned Azure resources radiating from the flow:
  - Azure Container Registry (ACR)
  - Azure Container Apps (ACA)
  - Azure Cosmos DB (database)
  - Azure Redis Cache (cache)
  - Application Insights (monitoring)

**Key Technology Icons:**
- Azure Developer CLI (azd logo)
- .NET Aspire (aspire symbol)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Cosmos DB (database icon)
- Azure Redis Cache (cache icon)
- Application Insights (chart icon)
- Bicep (infrastructure code icon)

**Text Elements:**
- Main title: "Azure Developer CLI (azd) with .NET Aspire" in bold sans-serif font
- Subtitle: "The Turnkey Solution - Azure Edition" below main title
- "One Command: azd up" badge near the central flow

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 9: AWS Copilot - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS Copilot: The Turnkey Container Solution - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS logo pattern in the background

**Central Elements:**
- Prominent AWS Copilot logo (copilot icon) at the center
- Flow visualization showing Copilot workflow:
  - `copilot init` (initialization icon)
  - `copilot env init` (environment icon)
  - `copilot deploy` (deploy icon)
- Auto-provisioned AWS resources radiating from the flow:
  - Amazon ECR (ECR icon)
  - Amazon ECS with Fargate (ECS icon)
  - Amazon DocumentDB (database icon)
  - ElastiCache Redis (cache icon)
  - Application Load Balancer (ALB icon)

**Key Technology Icons:**
- AWS Copilot (copilot icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon DocumentDB (database icon)
- ElastiCache Redis (cache icon)
- Application Load Balancer (ALB icon)
- AWS Secrets Manager (key icon)

**Text Elements:**
- Main title: "AWS Copilot" in bold sans-serif font
- Subtitle: "The Turnkey Container Solution - AWS Edition" below main title
- "Graviton Optimized" badge near the ECS icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 10: Visual Studio GUI Publishing - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio 2026 GUI Publishing: Drag-and-Drop Azure Deployments" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Visual Studio logo pattern in the background

**Central Elements:**
- Prominent Visual Studio 2026 logo (purple) on left side
- Azure logo (blue) on right side
- Visual representation of the GUI workflow:
  - Right-click project
  - Publish wizard
  - Select Azure Container Registry
  - Deploy
- Container Tools window visualization showing running containers and images

**Key Technology Icons:**
- Visual Studio (VS logo)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Docker (whale icon)
- Podman (leaf icon)
- Hot Reload (refresh icon)

**Text Elements:**
- Main title: "Visual Studio 2026 GUI Publishing" in bold sans-serif font
- Subtitle: "Drag-and-Drop Azure Deployments" below main title
- "Hot Reload Inside Containers" badge near the refresh icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Visual Studio purple accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 11: Visual Studio AWS Toolkit - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio 2026 GUI Publishing: Drag-and-Drop AWS Deployments - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Visual Studio logo pattern in the background

**Central Elements:**
- Prominent Visual Studio 2026 logo (purple) on left side
- AWS logo (orange) on right side
- Visual representation of the AWS Toolkit GUI workflow:
  - AWS Explorer window
  - Right-click project
  - Publish to Amazon ECR
  - Deploy to ECS
- AWS Explorer visualization showing ECR repositories and ECS services

**Key Technology Icons:**
- Visual Studio (VS logo)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Toolkit (AWS logo with VS)
- AWS Copilot (copilot icon)
- CloudFormation (stack icon)

**Text Elements:**
- Main title: "Visual Studio 2026 GUI Publishing" in bold sans-serif font
- Subtitle: "Drag-and-Drop AWS Deployments - AWS Edition" below main title
- "AWS Toolkit" badge near the AWS logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 12: Tarball Export + Runtime Load - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (blue)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to ACR (cloud icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Microsoft Defender for Cloud (Defender logo)
- Azure Key Vault (key icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - Azure Edition" below main title
- "Air-Gapped Support" badge near the approval gate

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 13: Tarball Export + Runtime Load - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (orange)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to Amazon ECR (ECR icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Amazon Inspector (Inspector logo)
- AWS Signer (signature icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - AWS Edition" below main title
- "Air-Gapped Support" badge near the approval gate

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 14: Podman with .NET SDK Native Publishing - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Podman with .NET SDK Native Publishing: Hybrid Workflows - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle pattern showing merging of two different paths

**Central Elements:**
- Two paths merging into one:
  - Left path: .NET SDK Native (cube icon with .NET symbol)
  - Right path: Podman (leaf icon)
  - Merged path: Hybrid Workflow (merge icon)
- Visual representation of the hybrid workflow:
  - SDK builds image (cube)
  - Podman tests locally (leaf with test tube)
  - Podman pushes to Azure (cloud)

**Key Technology Icons:**
- .NET SDK (cube icon)
- Podman (leaf icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Multi-architecture emulation (chip icon)
- Rootless security (shield icon)

**Text Elements:**
- Main title: "Podman with .NET SDK Native Publishing" in bold sans-serif font
- Subtitle: "Hybrid Workflows - Azure Edition" below main title
- "Best of Both Worlds" badge near the merging point

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 15: Podman with .NET SDK Native Publishing - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Podman with .NET SDK Native Publishing: Hybrid Workflows - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle pattern showing merging of two different paths

**Central Elements:**
- Two paths merging into one:
  - Left path: .NET SDK Native (cube icon with .NET symbol)
  - Right path: Podman (leaf icon)
  - Merged path: Hybrid Workflow (merge icon)
- Visual representation of the hybrid workflow:
  - SDK builds image (cube)
  - Podman tests locally with Graviton emulation (leaf with chip)
  - Podman pushes to Amazon ECR (ECR icon)

**Key Technology Icons:**
- .NET SDK (cube icon)
- Podman (leaf icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Graviton emulation (chip icon)
- Rootless security (shield icon)

**Text Elements:**
- Main title: "Podman with .NET SDK Native Publishing" in bold sans-serif font
- Subtitle: "Hybrid Workflows - AWS Edition" below main title
- "Graviton Testing" badge near the emulation icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 16: konet - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "konet: Multi-Platform Container Builds Without Docker - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle gear/tool pattern representing konet's tooling nature

**Central Elements:**
- Prominent konet logo (tool/gear icon with rocket) at the center
- Visual representation of parallel multi-architecture builds:
  - AMD64 architecture chip icon (x64)
  - ARM64 architecture chip icon (ARM)
  - Both feeding into a multi-arch manifest (document icon)
- Flowing lines showing konet building both architectures simultaneously

**Key Technology Icons:**
- konet (tool icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- AMD64 chip icon (x64)
- ARM64 chip icon (Graviton)
- Multi-arch manifest (document icon)

**Text Elements:**
- Main title: "konet: Multi-Platform Container Builds Without Docker" in bold sans-serif font
- Subtitle: "Azure Edition" below main title
- "Parallel Architecture Builds" badge near the chips

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 17: konet - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "konet: Multi-Platform Container Builds Without Docker - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle gear/tool pattern representing konet's tooling nature

**Central Elements:**
- Prominent konet logo (tool/gear icon with rocket) at the center
- Visual representation of parallel multi-architecture builds for AWS:
  - AMD64 architecture chip icon (x64)
  - ARM64 architecture chip icon (AWS Graviton)
  - Both feeding into a multi-arch manifest (document icon)
- AWS Graviton chip prominently displayed with highlight

**Key Technology Icons:**
- konet (tool icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- AMD64 chip icon (x64)
- Multi-arch manifest (document icon)

**Text Elements:**
- Main title: "konet: Multi-Platform Container Builds Without Docker" in bold sans-serif font
- Subtitle: "AWS Edition" below main title
- "Graviton Optimized" badge near the ARM64 chip

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 18: Kubernetes Native Deployments - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Kubernetes Native Deployments: Orchestrating .NET 10 Containers at Scale - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Azure Kubernetes Service (AKS) logo connected to the helm
- Visual representation of AKS cluster architecture:
  - Control plane (API Server, etcd, Scheduler)
  - Worker nodes (multiple node icons)
  - Pods running .NET containers
  - Services and Ingress

**Key Technology Icons:**
- Kubernetes (helm icon)
- Azure Kubernetes Service (AKS logo)
- Azure Container Registry (ACR icon)
- Helm (helm icon)
- Horizontal Pod Autoscaler (scaling icon)
- Azure Monitor (monitoring icon)

**Text Elements:**
- Main title: "Kubernetes Native Deployments" in bold sans-serif font
- Subtitle: "Orchestrating .NET 10 Containers at Scale - Azure Edition" below main title
- "Azure Kubernetes Service" badge near the AKS logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 19: Kubernetes Native Deployments - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Kubernetes Native Deployments: Orchestrating .NET 10 Containers at Scale - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Amazon EKS logo connected to the helm
- Visual representation of EKS cluster architecture:
  - Control plane (AWS managed)
  - Managed node groups (multiple node icons)
  - Pods running .NET containers
  - ALB Ingress Controller

**Key Technology Icons:**
- Kubernetes (helm icon)
- Amazon EKS (EKS logo)
- Amazon ECR (ECR icon)
- AWS Load Balancer Controller (ALB icon)
- Helm (helm icon)
- Horizontal Pod Autoscaler (scaling icon)

**Text Elements:**
- Main title: "Kubernetes Native Deployments" in bold sans-serif font
- Subtitle: "Orchestrating .NET 10 Containers at Scale - AWS Edition" below main title
- "Amazon EKS" badge near the EKS logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```


# Series: Publishing .NET 10 Apps as Container Images - A Complete Guide to 10 Deployment Approaches - AWS

---

### Prompt 1: Main Series Banner - .NET 10 Containerization Guide - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for "Publishing .NET 10 Apps as Container Images: A Complete Guide to 10 Deployment Approaches - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle .NET logo watermark pattern in the background
- Subtle AWS logo pattern overlay in the background (very light, almost transparent)

**Central Elements:**
- Prominent .NET logo (purple/magenta) with "10" badge on left side
- AWS logo (orange) prominently displayed on right side
- Ten radial connectors emanating from the .NET logo, each leading to an icon representing one of the 10 deployment approaches
- The connectors should form a circular or semi-circular pattern around the .NET logo

**10 Deployment Approach Icons:**
1. **.NET SDK Native Container Publishing Deep Dive** - Book icon with .NET symbol
2. **.NET SDK Native Core** - Cube icon with .NET symbol
3. **Dockerfile with Docker** - Whale icon (Docker)
4. **Dockerfile with Podman** - Leaf icon (Podman)
5. **AWS CDK & Copilot** - AWS CDK logo with Copilot icon
6. **Visual Studio GUI** - Visual Studio logo with AWS Toolkit badge
7. **Tarball Export** - Archive/package icon with shield
8. **Podman + SDK Native** - Merge icon (two arrows combining)
9. **konet** - Tool/gear icon with rocket
10. **Amazon EKS** - Kubernetes helm icon with AWS

**AWS Technology Stack Row (bottom):**
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- AWS Graviton (chip icon)
- AWS Copilot (copilot icon)
- AWS CDK (CDK icon)
- AWS App Runner (App Runner icon)
- Amazon Inspector (shield icon)

**Text Elements:**
- Main title: "Publishing .NET 10 Apps as Container Images" in bold sans-serif font
- Subtitle: "A Complete Guide to 10 Deployment Approaches - AWS Edition" below main title
- "AWS Cloud" label near AWS logo
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos. Use a subtle, elegant font with slight opacity or a minimal badge style to ensure visibility without distracting from the main design.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle gradients
- 3D elements with soft shadows
- Flat design with slight depth
- High resolution (1920x1080 or larger)
```

---

### Prompt 2: .NET SDK Native Container Publishing Deep Dive - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story ".NET SDK Native Container Publishing Deep Dive: The Complete Reference - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle .NET logo pattern in the background
- Subtle AWS logo pattern overlay (very light, almost transparent)

**Central Elements:**
- Prominent .NET logo (purple/magenta) with "10" badge on left side
- AWS logo (orange) on right side
- Flowing connector lines between .NET and AWS logos
- Open book icon in the center representing "The Complete Reference"
- Visual representation of MSBuild properties table floating around the book:
  - ContainerRegistry property card
  - ContainerRepository property card
  - ContainerImageTags property card
  - PublishAot property card

**Key Technology Icons:**
- MSBuild (gear icon)
- Native AOT (lightning bolt icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- IAM Roles (role icon)
- AWS Secrets Manager (key icon)

**Text Elements:**
- Main title: ".NET SDK Native Container Publishing Deep Dive" in bold sans-serif font
- Subtitle: "The Complete Reference - AWS Edition" below main title
- "MSBuild Properties Reference" badge near the property cards

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 3: .NET SDK Native Container Publishing - Building OCI Images Without Docker - AWS

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story ".NET SDK Native Container Publishing: Building OCI Images Without Docker - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle .NET logo pattern in the background

**Central Elements:**
- .NET logo (purple/magenta) with "10" badge on left side
- AWS logo (orange) prominently displayed on right side
- Flowing connector lines between .NET and AWS logos
- Visual representation of OCI image layers stacked in the middle:
  - Base OS layer (Amazon Linux 2)
  - .NET Runtime layer
  - Application dependencies layer
  - Application code layer
- AWS Graviton chip icon integrated into the architecture

**Key Technology Icons:**
- MSBuild (gear icon)
- Native AOT (lightning bolt icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- AWS Graviton (chip icon)
- IAM Roles (role icon)

**Text Elements:**
- Main title: ".NET SDK Native Container Publishing" in bold sans-serif font
- Subtitle: "Building OCI Images Without Docker - AWS" below main title
- "AWS Cloud" text near AWS logo
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 4: Traditional Dockerfile with Docker - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Traditional Dockerfile with Docker: The Classic Approach - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Docker whale silhouette pattern in the background

**Central Elements:**
- Prominent Docker whale icon (blue) on left side
- AWS logo (orange) on right side
- Dockerfile document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (SDK)
  - Publish stage
  - Runtime stage (ASP.NET)
  - Final image

**Key Technology Icons:**
- Docker (whale icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- .NET SDK (cube icon)
- .NET Runtime (runtime icon)
- AWS Copilot (copilot icon)
- IAM Roles (role icon)

**Text Elements:**
- Main title: "Traditional Dockerfile with Docker" in bold sans-serif font
- Subtitle: "The Classic Approach - AWS" below main title
- "Multi-Stage Builds" badge near the layer visualization
- "ECR Integration" badge near the ECR icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 5: Traditional Dockerfile with Podman - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Traditional Dockerfile with Podman: The Daemonless Alternative - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle leaf/plant pattern representing Podman's rootless nature

**Central Elements:**
- Prominent Podman leaf icon (green) on left side
- AWS logo (orange) on right side
- Visual representation of rootless container architecture:
  - User namespace
  - Container process (rootless)
  - No central daemon
- Flowing connector lines between Podman and AWS logos
- EC2 instance profile integration visualization

**Key Technology Icons:**
- Podman (leaf icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- EC2 Instance Profile (EC2 icon)
- Rootless badge (shield with leaf)
- Amazon Linux 2023 (Linux icon)

**Text Elements:**
- Main title: "Traditional Dockerfile with Podman" in bold sans-serif font
- Subtitle: "The Daemonless Alternative - AWS" below main title
- "Rootless Security on EC2" badge near the architecture visualization

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents and green accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 6: AWS CDK & Copilot - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS CDK & Copilot: Infrastructure as Code for Containers - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS CDK and Copilot logo pattern in the background

**Central Elements:**
- Prominent AWS CDK logo (blue) on left side
- AWS Copilot logo (copilot icon) on right side
- .NET logo (purple/magenta) with "10" badge at the center
- Flowing connector lines connecting CDK and Copilot to the .NET logo
- Visual representation of infrastructure as code:
  - TypeScript/C# code snippet on one side
  - CloudFormation stack on the other side
  - ECS Fargate service in the middle

**Key Technology Icons:**
- AWS CDK (CDK logo)
- AWS Copilot (copilot icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- Application Load Balancer (ALB icon)
- AWS CloudFormation (stack icon)

**Text Elements:**
- Main title: "AWS CDK & Copilot" in bold sans-serif font
- Subtitle: "Infrastructure as Code for Containers - AWS" below main title
- "TypeScript / C#" badge near the code snippet

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 7: Visual Studio 2026 GUI Publishing - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio 2026 GUI Publishing: Drag-and-Drop AWS Deployments - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Visual Studio logo pattern in the background

**Central Elements:**
- Prominent Visual Studio 2026 logo (purple) on left side
- AWS logo (orange) on right side
- Visual representation of the AWS Toolkit GUI workflow:
  - AWS Explorer window
  - Right-click project
  - Publish to Amazon ECR
  - Deploy to ECS
- AWS Explorer visualization showing ECR repositories and ECS services

**Key Technology Icons:**
- Visual Studio (VS logo)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Toolkit (AWS logo with VS)
- AWS Copilot (copilot icon)
- CloudFormation (stack icon)
- Hot Reload (refresh icon)

**Text Elements:**
- Main title: "Visual Studio 2026 GUI Publishing" in bold sans-serif font
- Subtitle: "Drag-and-Drop AWS Deployments - AWS" below main title
- "AWS Toolkit" badge near the AWS logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 8: Tarball Export + Runtime Load - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (orange)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to Amazon ECR (ECR icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Amazon Inspector (Inspector logo)
- AWS Signer (signature icon)
- AWS Secrets Manager (key icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - AWS" below main title
- "Air-Gapped Support" badge near the approval gate
- "Amazon Inspector" badge near the Inspector logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 9: Podman with .NET SDK Native Publishing - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Podman with .NET SDK Native Publishing: Hybrid Workflows - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle pattern showing merging of two different paths

**Central Elements:**
- Two paths merging into one:
  - Left path: .NET SDK Native (cube icon with .NET symbol)
  - Right path: Podman (leaf icon)
  - Merged path: Hybrid Workflow (merge icon)
- Visual representation of the hybrid workflow:
  - SDK builds image (cube)
  - Podman tests locally with Graviton emulation (leaf with chip)
  - Podman pushes to Amazon ECR (ECR icon)

**Key Technology Icons:**
- .NET SDK (cube icon)
- Podman (leaf icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- Graviton emulation (chip with arrow)
- Rootless security (shield icon)

**Text Elements:**
- Main title: "Podman with .NET SDK Native Publishing" in bold sans-serif font
- Subtitle: "Hybrid Workflows - AWS" below main title
- "Graviton Testing" badge near the emulation icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 10: konet - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "konet: Multi-Platform Container Builds Without Docker - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle gear/tool pattern representing konet's tooling nature

**Central Elements:**
- Prominent konet logo (tool/gear icon with rocket) at the center
- Visual representation of parallel multi-architecture builds for AWS:
  - AMD64 architecture chip icon (x64)
  - ARM64 architecture chip icon (AWS Graviton)
  - Both feeding into a multi-arch manifest (document icon)
- AWS Graviton chip prominently displayed with highlight

**Key Technology Icons:**
- konet (tool icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- AMD64 chip icon (x64)
- Multi-arch manifest (document icon)
- AWS CodeBuild (CodeBuild icon)

**Text Elements:**
- Main title: "konet: Multi-Platform Container Builds Without Docker" in bold sans-serif font
- Subtitle: "AWS Edition" below main title
- "Graviton Optimized" badge near the ARM64 chip
- "Parallel Architecture Builds" badge near the chips

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 11: Amazon EKS - Kubernetes Native Deployments - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Kubernetes Native Deployments: Orchestrating .NET 10 Containers at Scale - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Amazon EKS logo connected to the helm
- Visual representation of EKS cluster architecture:
  - Control plane (AWS managed - API Server, etcd, Scheduler)
  - Managed node groups (multiple node icons with Graviton badge)
  - Pods running .NET 10 containers
  - ALB Ingress Controller (ALB icon)
  - AWS Load Balancer Controller

**Key Technology Icons:**
- Kubernetes (helm icon)
- Amazon EKS (EKS logo)
- Amazon ECR (ECR icon)
- AWS Load Balancer Controller (ALB icon)
- Helm (helm icon)
- Horizontal Pod Autoscaler (scaling icon)
- AWS Graviton (chip icon)
- AWS Secrets Manager (key icon)

**Text Elements:**
- Main title: "Kubernetes Native Deployments" in bold sans-serif font
- Subtitle: "Orchestrating .NET 10 Containers at Scale - AWS Edition" below main title
- "Amazon EKS" badge near the EKS logo
- "Graviton Node Groups" badge near the node icons

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Prompt 12: AWS App Runner - Fully Managed Node.js Container Service - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS App Runner: Fully Managed Node.js Container Service - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS App Runner logo pattern in the background

**Central Elements:**
- Prominent AWS App Runner logo (App Runner icon) at the center
- .NET 10 logo (purple/magenta) with "10" badge connected to App Runner
- Visual representation of App Runner's fully managed architecture:
  - Source (GitHub/ECR) icon
  - Automatic Build icon
  - Container Deployment icon
  - Auto Scaling icon
  - Built-in Load Balancer icon
  - Automatic SSL/TLS icon

**Key Technology Icons:**
- AWS App Runner (App Runner icon)
- Amazon ECR (ECR icon)
- GitHub (GitHub logo)
- CloudWatch (monitoring icon)
- AWS Secrets Manager (key icon)
- Auto Scaling (scaling icon)

**Text Elements:**
- Main title: "AWS App Runner" in bold sans-serif font
- Subtitle: "Fully Managed Node.js Container Service - AWS" below main title
- "Zero Infrastructure Management" badge near the center

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```