
# Complete Gemini Image Generation Prompts

## Python FastAPI Containerization Series - All 22 Stories (Azure + AWS)

## Main prompt
```
Great Now create the the gemini image generation prompt for:
1."Publishing Python FastAPI Apps as Container Images: A Complete Guide to 10 Deployment Approaches - AWS" story with appropriate style and logs with connectors (extract from story) with sub-title. Place "Vineet Sharma" at bottom Right
2. "Publishing Python FastAPI Apps as Container Images: A Complete Guide to 10 Deployment Approaches" story with appropriate style and logs with connectors (extract from story) with sub-title. Place "Vineet Sharma" at bottom Right
3. For individual concepts/stories for Azure sub-title
4. For individual concepts/stories for AWS sub-title
```
---

### Main Series Banners (2 Prompts)

---

#### Prompt 1: Main Series Banner - Python FastAPI Containerization Guide

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for "Publishing Python FastAPI Apps as Container Images: A Complete Guide to 10 Deployment Approaches" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e3f2fd)
- Subtle Python logo pattern in the background (snake silhouette, very light)

**Central Elements:**
- Prominent Python logo (blue/yellow) with "FastAPI" badge at the center
- Ten radial connectors emanating from the Python logo, each leading to an icon representing one of the 10 deployment approaches
- The connectors should form a circular or semi-circular pattern around the logo

**10 Deployment Approach Icons:**
1. **Poetry + Docker Multi-Stage** - Quill icon with container
2. **UV + Docker** - Lightning bolt icon with container
3. **Pip + Docker** - Package icon with container
4. **Azure Container Apps** - Azure container icon
5. **VS Code Dev Containers** - VS Code icon with container
6. **Azure Developer CLI (azd)** - Terminal with Azure logo
7. **Tarball Export** - Archive/package icon with shield
8. **Azure Kubernetes Service (AKS)** - AKS logo
9. **GitHub Actions** - GitHub logo
10. **AWS CDK & Copilot** - AWS CDK and Copilot icons

**Technology Stack Row (bottom):**
- FastAPI (lightning bolt with API text)
- Pydantic (data validation icon)
- Motor/MongoDB (database icon)
- Uvicorn (server icon)
- Poetry (quill icon)
- UV (fast icon)
- Docker (whale icon)
- Kubernetes (helm icon)

**Cloud Platform Icons:**
- Azure logo (blue) on left side
- AWS logo (orange) on right side
- Curved connector lines flowing from both cloud logos to the central Python logo

**Text Elements:**
- Main title: "Publishing Python FastAPI Apps as Container Images" in bold sans-serif font
- Subtitle: "A Complete Guide to 10 Deployment Approaches" below main title
- "Azure" and "AWS" labels near respective cloud logos
- "20 Stories" badge (10 Azure + 10 AWS) in the corner

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle gradients
- 3D elements with soft shadows
- Flat design with slight depth
- High resolution (1920x1080 or larger)
```

---

#### Prompt 2: Main Series Banner - Python FastAPI Containerization Guide - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for "Publishing Python FastAPI Apps as Container Images: A Complete Guide to 10 Deployment Approaches - AWS Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Python logo pattern in the background
- Subtle AWS logo pattern overlay (very light, almost transparent)

**Central Elements:**
- Prominent Python logo (blue/yellow) with "FastAPI" badge on left side
- AWS logo (orange) prominently displayed on right side
- Ten radial connectors emanating from the Python logo, each leading to an icon representing one of the 10 AWS deployment approaches
- The connectors should form a circular or semi-circular pattern around the Python logo

**10 Deployment Approach Icons:**
1. **Poetry + Docker Multi-Stage** - Quill icon with container
2. **UV + Docker** - Lightning bolt icon with container
3. **Pip + Docker** - Package icon with container
4. **AWS Copilot** - Copilot icon
5. **VS Code Dev Containers** - VS Code icon with AWS Toolkit
6. **AWS CDK with Python** - AWS CDK logo with Python
7. **Tarball Export** - Archive/package icon with shield
8. **Amazon EKS** - EKS logo
9. **GitHub Actions + ECR** - GitHub logo with ECR icon
10. **AWS App Runner** - App Runner icon

**AWS Technology Stack Row (bottom):**
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Amazon EKS (EKS icon)
- AWS Graviton (chip icon)
- AWS Copilot (copilot icon)
- AWS CDK (CDK icon)
- AWS App Runner (App Runner icon)
- Amazon Inspector (shield icon)

**Text Elements:**
- Main title: "Publishing Python FastAPI Apps as Container Images" in bold sans-serif font
- Subtitle: "A Complete Guide to 10 Deployment Approaches - AWS Edition" below main title
- "AWS Cloud" label near AWS logo
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### Azure Edition (10 Stories)

---

#### Prompt 3: Poetry + Docker Multi-Stage - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Poetry + Docker Multi-Stage: The Modern Python Approach - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Poetry logo pattern in the background (quill with Python)

**Central Elements:**
- Prominent Poetry icon (quill) on left side
- Azure logo (blue) on right side
- Dockerfile document icon with pyproject.toml and poetry.lock files in the center
- Visual representation of multi-stage build layers:
  - Build stage (poetry install)
  - Install dependencies layer
  - Runtime stage (Python slim)
  - Final image

**Key Technology Icons:**
- Poetry (quill icon)
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Python (Python logo)
- FastAPI (lightning bolt with API)

**Text Elements:**
- Main title: "Poetry + Docker Multi-Stage" in bold sans-serif font
- Subtitle: "The Modern Python Approach - Azure Edition" below main title
- "Deterministic Builds" badge near the lock file icon
- "pyproject.toml" badge

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 4: UV + Docker - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "UV + Docker: Blazing Fast Python Package Management - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle lightning bolt pattern representing UV's speed

**Central Elements:**
- Prominent UV icon (lightning bolt) on left side
- Azure logo (blue) on right side
- Visual representation of UV's blazing fast dependency resolution:
  - Sub-second resolution speedometer
  - Parallel downloads visualization
  - Rust-based engine icon
  - Lock file with checkmark

**Key Technology Icons:**
- UV (lightning bolt icon)
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Rust (gear icon)
- FastAPI (lightning bolt with API)
- Python (Python logo)

**Text Elements:**
- Main title: "UV + Docker" in bold sans-serif font
- Subtitle: "Blazing Fast Python Package Management - Azure Edition" below main title
- "10-100x Faster" badge near the speedometer
- "Rust-Powered" badge near the gear icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 5: Pip + Docker - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Pip + Docker: The Classic Python Containerization - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle pip logo pattern in the background

**Central Elements:**
- Prominent pip icon (package with down arrow) on left side
- Azure logo (blue) on right side
- requirements.txt document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (pip install)
  - Install dependencies layer
  - Runtime stage (Python slim)
  - Final image

**Key Technology Icons:**
- pip (package icon)
- Docker (whale icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Python (Python logo)
- FastAPI (lightning bolt with API)

**Text Elements:**
- Main title: "Pip + Docker" in bold sans-serif font
- Subtitle: "The Classic Python Containerization - Azure Edition" below main title
- "Universal Compatibility" badge near requirements.txt
- "Battle-Tested" badge

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 6: Azure Container Apps - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Container Apps: Serverless Python Deployment" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Azure Container Apps logo pattern in the background

**Central Elements:**
- Prominent Azure Container Apps (ACA) logo at the center
- Python logo (blue/yellow) with "FastAPI" badge connected to ACA
- Visual representation of ACA's serverless architecture:
  - Auto-scaling rules (scale up/down arrows)
  - Revision management (version icons)
  - Ingress configuration (traffic flow)
  - Scale to zero (zero icon)

**Key Technology Icons:**
- Azure Container Apps (ACA logo)
- Azure Container Registry (ACR icon)
- Azure Monitor (monitoring icon)
- KEDA (KEDA icon)
- Dapr (Dapr icon)

**Text Elements:**
- Main title: "Azure Container Apps" in bold sans-serif font
- Subtitle: "Serverless Python Deployment" below main title
- "Scale to Zero" badge near the zero icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 7: Visual Studio Code Dev Containers - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio Code Dev Containers: Local Development to Production - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle VS Code logo pattern in the background

**Central Elements:**
- Prominent VS Code logo (blue) on left side
- Azure logo (blue) on right side
- Dev container visualization in the center:
  - Container icon with VS Code inside
  - Python environment configuration
  - Port forwarding arrows
- LocalStack emulator icon for Azure service emulation

**Key Technology Icons:**
- VS Code (VS Code logo)
- Dev Containers (container with code icon)
- Docker (whale icon)
- Azure CLI (terminal icon)
- Python (Python logo)
- FastAPI (lightning bolt with API)

**Text Elements:**
- Main title: "Visual Studio Code Dev Containers" in bold sans-serif font
- Subtitle: "Local Development to Production - Azure Edition" below main title
- "Consistent Python Environments" badge near the dev container

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 8: Azure Developer CLI (azd) - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Developer CLI (azd) with Python: The Turnkey Solution - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Azure logo pattern in the background

**Central Elements:**
- Azure Developer CLI (azd) logo (terminal with Azure symbol) prominently displayed
- Python logo (blue/yellow) with "FastAPI" badge connected to the azd logo
- Flow visualization showing the three phases of `azd up`:
  1. Package (container icon)
  2. Provision (infrastructure icon)
  3. Deploy (cloud with arrow)
- Auto-provisioned Azure resources radiating from the flow:
  - Azure Container Registry (ACR)
  - Azure Container Apps (ACA)
  - Azure Cosmos DB (database)
  - Azure Redis Cache (cache)
  - Application Insights (monitoring)

**Key Technology Icons:**
- Azure Developer CLI (azd logo)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Azure Cosmos DB (database icon)
- Bicep (infrastructure code icon)

**Text Elements:**
- Main title: "Azure Developer CLI (azd) with Python" in bold sans-serif font
- Subtitle: "The Turnkey Solution - Azure Edition" below main title
- "One Command: azd up" badge near the central flow

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 9: Tarball Export - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (blue)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to ACR (cloud icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Microsoft Defender for Cloud (Defender logo)
- Azure Key Vault (key icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - Azure Edition" below main title
- "Air-Gapped Support" badge near the approval gate

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 10: Azure Kubernetes Service (AKS) - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Azure Kubernetes Service (AKS): Python Microservices at Scale" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Azure Kubernetes Service (AKS) logo connected to the helm
- Visual representation of AKS cluster architecture:
  - Control plane (API Server, etcd, Scheduler)
  - Worker nodes (multiple node icons)
  - Pods running Python FastAPI containers
  - Services and Ingress

**Key Technology Icons:**
- Kubernetes (helm icon)
- Azure Kubernetes Service (AKS logo)
- Azure Container Registry (ACR icon)
- Helm (helm icon)
- Horizontal Pod Autoscaler (scaling icon)
- Azure Monitor (monitoring icon)

**Text Elements:**
- Main title: "Azure Kubernetes Service (AKS)" in bold sans-serif font
- Subtitle: "Python Microservices at Scale" below main title
- "Azure Kubernetes Service" badge near the AKS logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 11: GitHub Actions + Container Registry - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "GitHub Actions + Container Registry: CI/CD for Python - Azure Edition" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle GitHub logo pattern in the background

**Central Elements:**
- Prominent GitHub logo (dark) on left side
- Azure logo (blue) on right side
- Azure Container Registry (ACR) icon in the center
- CI/CD workflow visualization:
  - Code push (Git commit icon)
  - GitHub Actions workflow (gear icon)
  - Docker build (container icon)
  - Trivy security scan (shield icon)
  - Push to ACR (upload icon)
  - Deploy to ACA (deploy icon)

**Key Technology Icons:**
- GitHub (GitHub logo)
- GitHub Actions (workflow icon)
- Azure Container Registry (ACR icon)
- Azure Container Apps (ACA icon)
- Trivy (trivy logo)
- OIDC (identity icon)

**Text Elements:**
- Main title: "GitHub Actions + Container Registry" in bold sans-serif font
- Subtitle: "CI/CD for Python - Azure Edition" below main title
- "OIDC Authentication" badge near the identity icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle Azure blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 12: AWS CDK & Copilot - Multi-Cloud - Azure Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS CDK & Copilot: Multi-Cloud Python Container Deployments" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light blue (#e7f1ff)
- Subtle AWS and Azure logo pattern in the background

**Central Elements:**
- AWS CDK logo (blue) on left side
- AWS Copilot icon (copilot) on right side
- Python logo (blue/yellow) with "FastAPI" badge at the center
- Flowing connector lines between AWS CDK, Copilot, and Python
- Azure logo integrated to show multi-cloud capability
- Visual representation of multi-cloud deployment:
  - Python CDK code snippet on one side
  - CloudFormation stack on the other side
  - ECS Fargate service with Azure Container Apps alternative

**Key Technology Icons:**
- AWS CDK (CDK logo)
- AWS Copilot (copilot icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Azure logo (Azure icon)
- Python (Python logo)

**Text Elements:**
- Main title: "AWS CDK & Copilot" in bold sans-serif font
- Subtitle: "Multi-Cloud Python Container Deployments" below main title
- "Azure + AWS" badge near the center

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle blue accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

### AWS Edition (10 Stories)

---

#### Prompt 13: Poetry + Docker Multi-Stage - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Poetry + Docker Multi-Stage: The Modern Python Approach - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Poetry logo pattern in the background

**Central Elements:**
- Prominent Poetry icon (quill) on left side
- AWS logo (orange) on right side
- Dockerfile document icon with pyproject.toml and poetry.lock files in the center
- AWS Graviton chip icon integrated into the architecture
- Visual representation of multi-stage build layers:
  - Build stage (poetry install)
  - Install dependencies layer
  - Runtime stage (Python slim)
  - Final image

**Key Technology Icons:**
- Poetry (quill icon)
- Docker (whale icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- Python (Python logo)

**Text Elements:**
- Main title: "Poetry + Docker Multi-Stage" in bold sans-serif font
- Subtitle: "The Modern Python Approach - AWS" below main title
- "Graviton Optimized" badge near the chip icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 14: UV + Docker - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "UV + Docker: Blazing Fast Python Package Management - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle lightning bolt pattern representing UV's speed

**Central Elements:**
- Prominent UV icon (lightning bolt) on left side
- AWS logo (orange) on right side
- Visual representation of UV's blazing fast dependency resolution:
  - Sub-second resolution speedometer
  - Parallel downloads visualization
  - Rust-based engine icon
  - AWS Graviton integration
  - AWS CodeBuild optimization badge

**Key Technology Icons:**
- UV (lightning bolt icon)
- Docker (whale icon)
- Amazon ECR (ECR icon)
- AWS CodeBuild (CodeBuild icon)
- AWS Graviton (chip icon)
- Python (Python logo)

**Text Elements:**
- Main title: "UV + Docker" in bold sans-serif font
- Subtitle: "Blazing Fast Python Package Management - AWS" below main title
- "CodeBuild Optimized" badge near the CodeBuild icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 15: Pip + Docker - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Pip + Docker: The Classic Python Containerization - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle pip logo pattern in the background

**Central Elements:**
- Prominent pip icon (package with down arrow) on left side
- AWS logo (orange) on right side
- requirements.txt document icon in the center with flowing lines connecting to both sides
- Visual representation of multi-stage build layers:
  - Build stage (pip install)
  - Install dependencies layer
  - Runtime stage (Python slim)
  - Final image

**Key Technology Icons:**
- pip (package icon)
- Docker (whale icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Python (Python logo)
- FastAPI (lightning bolt with API)

**Text Elements:**
- Main title: "Pip + Docker" in bold sans-serif font
- Subtitle: "The Classic Python Containerization - AWS" below main title
- "Universal Compatibility" badge near requirements.txt

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 16: AWS Copilot - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS Copilot: The Turnkey Container Solution - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS Copilot logo pattern in the background

**Central Elements:**
- Prominent AWS Copilot logo (copilot icon) at the center
- Python logo (blue/yellow) with "FastAPI" badge connected to Copilot
- Flow visualization showing Copilot workflow:
  - `copilot init` (initialization icon)
  - `copilot env init` (environment icon)
  - `copilot deploy` (deploy icon)
- Auto-provisioned AWS resources radiating from the flow:
  - Amazon ECR (ECR icon)
  - Amazon ECS with Fargate (ECS icon)
  - Amazon DocumentDB (database icon)
  - ElastiCache Redis (cache icon)
  - Application Load Balancer (ALB icon)

**Key Technology Icons:**
- AWS Copilot (copilot icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- AWS Graviton (chip icon)
- AWS Secrets Manager (key icon)

**Text Elements:**
- Main title: "AWS Copilot" in bold sans-serif font
- Subtitle: "The Turnkey Container Solution - AWS" below main title
- "Graviton Optimized" badge near the ECS icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 17: Visual Studio Code Dev Containers - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Visual Studio Code Dev Containers: Local Development to Production - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle VS Code logo pattern in the background

**Central Elements:**
- Prominent VS Code logo (blue) on left side
- AWS logo (orange) on right side
- Dev container visualization in the center:
  - Container icon with VS Code inside
  - Python environment configuration
  - Port forwarding arrows
- LocalStack emulator icon for AWS service emulation (Secrets Manager, S3, SQS)

**Key Technology Icons:**
- VS Code (VS Code logo)
- Dev Containers (container with code icon)
- Docker (whale icon)
- AWS CLI (terminal icon)
- LocalStack (stack icon)
- AWS Toolkit (AWS logo with VS)

**Text Elements:**
- Main title: "Visual Studio Code Dev Containers" in bold sans-serif font
- Subtitle: "Local Development to Production - AWS" below main title
- "LocalStack Integration" badge near the stack icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 18: AWS CDK with Python - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS CDK with Python: Infrastructure as Code for Containers - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS CDK logo pattern in the background

**Central Elements:**
- Prominent AWS CDK logo (blue) on left side
- Python logo (blue/yellow) on right side
- FastAPI logo (lightning bolt with API) at the center
- Flowing connector lines between CDK, Python, and FastAPI
- Visual representation of infrastructure as code:
  - Python CDK code snippet on one side
  - CloudFormation stack on the other side
  - ECS Fargate service in the middle

**Key Technology Icons:**
- AWS CDK (CDK logo)
- Python (Python logo)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Application Load Balancer (ALB icon)
- AWS CloudFormation (stack icon)

**Text Elements:**
- Main title: "AWS CDK with Python" in bold sans-serif font
- Subtitle: "Infrastructure as Code for Containers - AWS" below main title
- "Python CDK" badge near the Python logo

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 19: Tarball Export - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Tarball Export + Runtime Load: Security-First CI/CD Workflows - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle shield pattern representing security

**Central Elements:**
- Security shield icon at the center (orange)
- Flow visualization showing the security-first workflow:
  1. Build → Container Tarball (archive icon)
  2. Security Scanning (shield with magnifying glass)
  3. Approval Gate (checkmark icon)
  4. Load to Docker/Podman (container icon)
  5. Push to Amazon ECR (ECR icon)

**Security Tool Icons:**
- Trivy (trivy logo)
- Grype (grype logo)
- Syft (syft logo)
- Amazon Inspector (Inspector logo)
- AWS Signer (signature icon)

**Text Elements:**
- Main title: "Tarball Export + Runtime Load" in bold sans-serif font
- Subtitle: "Security-First CI/CD Workflows - AWS" below main title
- "Air-Gapped Support" badge near the approval gate

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 20: Amazon EKS - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "Amazon EKS: Python Microservices at Scale - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle Kubernetes helm icon pattern in the background

**Central Elements:**
- Prominent Kubernetes helm icon (steering wheel) at the center
- Amazon EKS logo connected to the helm
- Visual representation of EKS cluster architecture:
  - Control plane (AWS managed)
  - Managed node groups (multiple node icons with Graviton badge)
  - Pods running Python FastAPI containers
  - ALB Ingress Controller (ALB icon)

**Key Technology Icons:**
- Kubernetes (helm icon)
- Amazon EKS (EKS logo)
- Amazon ECR (ECR icon)
- AWS Load Balancer Controller (ALB icon)
- AWS Graviton (chip icon)
- Helm (helm icon)

**Text Elements:**
- Main title: "Amazon EKS" in bold sans-serif font
- Subtitle: "Python Microservices at Scale - AWS" below main title
- "Amazon EKS" badge near the EKS logo
- "Graviton Node Groups" badge near the node icons

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 21: GitHub Actions + Amazon ECR - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "GitHub Actions + Amazon ECR: CI/CD for Python - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle GitHub logo pattern in the background

**Central Elements:**
- Prominent GitHub logo (dark) on left side
- AWS logo (orange) on right side
- Amazon ECR icon in the center
- CI/CD workflow visualization:
  - Code push (Git commit icon)
  - GitHub Actions workflow (gear icon)
  - Docker build (container icon)
  - Trivy security scan (shield icon)
  - Push to ECR (upload icon)
  - Deploy to ECS (deploy icon)

**Key Technology Icons:**
- GitHub (GitHub logo)
- GitHub Actions (workflow icon)
- Amazon ECR (ECR icon)
- Amazon ECS with Fargate (ECS icon)
- Trivy (trivy logo)
- OIDC (identity icon)

**Text Elements:**
- Main title: "GitHub Actions + Amazon ECR" in bold sans-serif font
- Subtitle: "CI/CD for Python - AWS" below main title
- "OIDC Authentication" badge near the identity icon

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

#### Prompt 22: AWS App Runner - AWS Edition

```markdown
Create a wide horizontal banner image (16:9 aspect ratio) for the story "AWS App Runner: Fully Managed Python Container Service - AWS" with a light, open-source style background. The design should feature:

**Background:**
- Light gradient from off-white (#f8f9fa) to light orange (#fff5e6)
- Subtle AWS App Runner logo pattern in the background

**Central Elements:**
- Prominent AWS App Runner logo (App Runner icon) at the center
- Python logo (blue/yellow) with "FastAPI" badge connected to App Runner
- Visual representation of App Runner's fully managed architecture:
  - Source (GitHub/ECR) icon
  - Automatic Build icon
  - Container Deployment icon
  - Auto Scaling icon
  - Built-in Load Balancer icon
  - Automatic SSL/TLS icon

**Key Technology Icons:**
- AWS App Runner (App Runner icon)
- Amazon ECR (ECR icon)
- GitHub (GitHub logo)
- CloudWatch (monitoring icon)
- AWS Secrets Manager (key icon)
- Auto Scaling (scaling icon)

**Text Elements:**
- Main title: "AWS App Runner" in bold sans-serif font
- Subtitle: "Fully Managed Python Container Service - AWS" below main title
- "Zero Infrastructure Management" badge near the center

**Attribution:**
- **"Vineet Sharma" in bold font at the bottom right corner**, placed in a clean area not overlapping with any icons, text, or logos.

**Style:**
- Clean, modern open-source aesthetic
- Light background with subtle AWS orange accents
- 3D elements with soft shadows
- High resolution (1920x1080 or larger)
```

---

This completes all **22 prompts** for the Python FastAPI Containerization series:
- **2 Main Series Banners** (1 Azure/General + 1 AWS Edition)
- **10 Azure Edition Stories**
- **10 AWS Edition Stories**

Each prompt includes:
- Unique subtitle and styling for each story
- Appropriate icons, connectors, and technology stack elements
- Cloud-specific accents (Azure blue for Azure editions, AWS orange for AWS editions)
- Attribution **"Vineet Sharma" in bold font at the bottom right corner**