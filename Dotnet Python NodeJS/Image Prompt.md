# Microservices Architecture Series - Banner Image Prompts

## From REST to gRPC: Architecting High-Performance APIs in .NET 10
```
Design a technical illustration in Microsoft Fluent Design style. Foreground shows architectural comparison: classical REST temple structure with JSON scrolls and HTTP pillars transforming into modern gRPC high-speed railway with Protobuf cargo containers and bidirectional train tracks. Background features cloud infrastructure, Kubernetes nodes, and database clusters. Technology logos integrated as neon signs: .NET 10 glowing purple, gRPC electric blue, Protocol Buffers as data cubes. Data flows as luminous blue streams connecting all components. Connectors include API gateway arches and service mesh network lines. Color palette: deep navy base with Microsoft brand colors accenting key elements. Modern, clean, corporate technical art style suitable for conference presentations. Signature "Vineet Sharma" discreetly placed at bottom left corner.
```

## From REST to gRPC: Architecting High-Performance APIs in Node.js

```
Create a modern, light-themed open source style architectural diagram showing the evolution from REST to gRPC in Python. Include a prominent title at the top center: "FROM REST TO gRPC: ARCHITECTING HIGH-PERFORMANCE APIs IN PYTHON" in bold, clean sans-serif font (24pt). Below the title, include a subtitle: "A 20-Minute Architectural Deep Dive | Fleet Management System Use Case" in medium weight, 14pt, with Python blue accent.

Left side depicts a REST API architecture with FastAPI logo, JSON payload visualization shown as curly braces with document icons, HTTP methods (GET, POST, PUT, DELETE) in soft green pill-shaped badges, and polling patterns shown as gentle dashed circular arrows in light gray. Right side depicts a gRPC architecture with Python-powered gRPC shown as flowing binary streams, Protocol Buffer binary format represented as compact hex blocks (08 65 12 05) with soft blue glow, bidirectional streaming represented by flowing arrows in both directions in vibrant teal, and persistent connections shown as smooth, flowing tubes with gradient. Center shows a transformation bridge with Python logo and subtle gradient transition.

Include technology logos: Python (yellow/blue), FastAPI (green), Pydantic (blue), SQLAlchemy (gray/blue), gRPC (blue), Protocol Buffers (light green), Redis (red), PostgreSQL (blue elephant), HTTP/2 (dark gray), asyncpg (teal), and OpenTelemetry (light blue). Visual connectors include flowing data streams in soft gradients, API gateways as gentle arches, and service mesh indicators as subtle dotted connections.

Color palette: clean white background, soft grays (#F5F5F5, #E0E0E0), Python blue (#3776AB), FastAPI green (#009688), gRPC blue (#00B5E2), with accent teal (#00ACC1). Light open source style with subtle shadows, clean rounded rectangles, friendly geometric shapes, and professional tech illustration aesthetic.

Text "Vineet Sharma" positioned at bottom left corner in clean sans-serif font (Inter or Roboto), 12pt, medium gray (#6C757D) with subtle background transparency. Include a small Python logo icon next to the signature.

16:9 landscape orientation, high resolution (3840×2160), suitable for conference presentations, technical documentation, and blog posts.
```