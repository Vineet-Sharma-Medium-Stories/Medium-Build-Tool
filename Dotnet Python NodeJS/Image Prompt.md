# Microservices Architecture Series - Banner Image Prompts

## From REST to gRPC: Architecting High-Performance APIs in .NET 10
```
Design a technical illustration in Microsoft Fluent Design style. Foreground shows architectural comparison: classical REST temple structure with JSON scrolls and HTTP pillars transforming into modern gRPC high-speed railway with Protobuf cargo containers and bidirectional train tracks. Background features cloud infrastructure, Kubernetes nodes, and database clusters. Technology logos integrated as neon signs: .NET 10 glowing purple, gRPC electric blue, Protocol Buffers as data cubes. Data flows as luminous blue streams connecting all components. Connectors include API gateway arches and service mesh network lines. Color palette: deep navy base with Microsoft brand colors accenting key elements. Modern, clean, corporate technical art style suitable for conference presentations. Signature "Vineet Sharma" discreetly placed at bottom left corner.
```

## From REST to gRPC: Architecting High-Performance APIs in Python

```
Create a modern, light-themed open source style architectural diagram showing the evolution from REST to gRPC in Python. Include a prominent title at the top center: "FROM REST TO gRPC: ARCHITECTING HIGH-PERFORMANCE APIs IN PYTHON" in bold, clean sans-serif font (24pt). Below the title, include a subtitle: "A 20-Minute Architectural Deep Dive | Fleet Management System Use Case" in medium weight, 14pt, with Python blue accent.

Left side depicts a REST API architecture with FastAPI logo, JSON payload visualization shown as curly braces with document icons, HTTP methods (GET, POST, PUT, DELETE) in soft green pill-shaped badges, and polling patterns shown as gentle dashed circular arrows in light gray. Right side depicts a gRPC architecture with Python-powered gRPC shown as flowing binary streams, Protocol Buffer binary format represented as compact hex blocks (08 65 12 05) with soft blue glow, bidirectional streaming represented by flowing arrows in both directions in vibrant teal, and persistent connections shown as smooth, flowing tubes with gradient. Center shows a transformation bridge with Python logo and subtle gradient transition.

Include technology logos: Python (yellow/blue), FastAPI (green), Pydantic (blue), SQLAlchemy (gray/blue), gRPC (blue), Protocol Buffers (light green), Redis (red), PostgreSQL (blue elephant), HTTP/2 (dark gray), asyncpg (teal), and OpenTelemetry (light blue). Visual connectors include flowing data streams in soft gradients, API gateways as gentle arches, and service mesh indicators as subtle dotted connections.

Color palette: clean white background, soft grays (#F5F5F5, #E0E0E0), Python blue (#3776AB), FastAPI green (#009688), gRPC blue (#00B5E2), with accent teal (#00ACC1). Light open source style with subtle shadows, clean rounded rectangles, friendly geometric shapes, and professional tech illustration aesthetic.

Text "Vineet Sharma" positioned at bottom left corner in clean sans-serif font (Inter or Roboto), 12pt, medium gray (#6C757D) with subtle background transparency. Include a small Python logo icon next to the signature.

16:9 landscape orientation, high resolution (3840×2160), suitable for conference presentations, technical documentation, and blog posts.
```
## From REST to gRPC: Architecting High-Performance APIs in Node.js

```
Create a modern, light-themed open source style architectural diagram showing the evolution from REST to gRPC in Python. Include a prominent title at the top center: "FROM REST TO gRPC: ARCHITECTING HIGH-PERFORMANCE APIs IN PYTHON" in bold, clean sans-serif font (24pt). Below the title, include a subtitle: "A 20-Minute Architectural Deep Dive | Fleet Management System Use Case" in medium weight, 14pt, with Python blue accent.

Left side depicts a REST API architecture with FastAPI logo, JSON payload visualization shown as curly braces with document icons, HTTP methods (GET, POST, PUT, DELETE) in soft green pill-shaped badges, and polling patterns shown as gentle dashed circular arrows in light gray. Right side depicts a gRPC architecture with Python-powered gRPC shown as flowing binary streams, Protocol Buffer binary format represented as compact hex blocks (08 65 12 05) with soft blue glow, bidirectional streaming represented by flowing arrows in both directions in vibrant teal, and persistent connections shown as smooth, flowing tubes with gradient. Center shows a transformation bridge with Python logo and subtle gradient transition.

Include technology logos: Python (yellow/blue), FastAPI (green), Pydantic (blue), SQLAlchemy (gray/blue), gRPC (blue), Protocol Buffers (light green), Redis (red), PostgreSQL (blue elephant), HTTP/2 (dark gray), asyncpg (teal), and OpenTelemetry (light blue). Visual connectors include flowing data streams in soft gradients, API gateways as gentle arches, and service mesh indicators as subtle dotted connections.

Color palette: clean white background, soft grays (#F5F5F5, #E0E0E0), Python blue (#3776AB), FastAPI green (#009688), gRPC blue (#00B5E2), with accent teal (#00ACC1). Light open source style with subtle shadows, clean rounded rectangles, friendly geometric shapes, and professional tech illustration aesthetic.

Text "Vineet Sharma" positioned at bottom left corner in clean sans-serif font (Inter or Roboto), 12pt, medium gray (#6C757D) with subtle background transparency. Include a small Python logo icon next to the signature.

16:9 landscape orientation, high resolution (3840×2160), suitable for conference presentations, technical documentation, and blog posts.
```
## From REST to gRPC: A Unified Architecture Across .NET 10, Node.js, and Python

```
Create a comprehensive, light-themed open source style architectural diagram showing a unified polyglot gRPC architecture spanning three platforms: .NET 10, Node.js, and Python. Include a prominent title at the top center: "FROM REST TO GRPC: A UNIFIED ARCHITECTURE ACROSS .NET 10, NODE.JS, AND PYTHON" in bold, clean sans-serif font (26pt). Below the title, include a subtitle: "Polyglot Fleet Management System | 30-Minute Architectural Cover Story" in medium weight, 16pt, with gradient accent blending .NET purple, Node.js green, and Python blue.

The diagram should be divided into three vertical columns, each representing one platform with its distinctive branding and technology stack:

LEFT COLUMN (33%): .NET 10 Architecture
- Background: Light purple gradient (#F3E5F5 to #FFFFFF)
- Header: ".NET 10" with .NET purple logo (#512BD4) and Native AOT badge
- Technology logos: ASP.NET Core (blue/purple), Entity Framework Core (blue/green), Hybrid Cache (purple icon), C# (purple/gray)
- Visual elements: Native AOT lightning bolt icon, 25ms cold start badge, 50MB memory badge
- gRPC components: Kestrel HTTP/2 server, IAsyncEnumerable streaming, ChannelReader/Writer
- Data flow: Purple gradient arrows connecting to Redis and PostgreSQL
- Performance metrics badge: "50,000 req/sec | 5ms P99 latency"

CENTER COLUMN (34%): Node.js Architecture
- Background: Light green gradient (#E8F5E9 to #FFFFFF)
- Header: "Node.js" with Node.js green logo (#68A063) and Event-Driven badge
- Technology logos: Fastify (orange), TypeScript (blue), Prisma (blue/white), BullMQ (blue/gray)
- Visual elements: Event loop visualization (circular arrows), EventEmitter icon, 5,000+ concurrent streams badge
- gRPC components: @grpc/grpc-js server, Duplex streams, async/await patterns
- Data flow: Green gradient arrows connecting to Redis and PostgreSQL
- Performance metrics badge: "35,000 req/sec | 8ms P99 latency"

RIGHT COLUMN (33%): Python Architecture
- Background: Light blue gradient (#E3F2FD to #FFFFFF)
- Header: "Python" with Python blue logo (#3776AB) and ML-Powered badge
- Technology logos: FastAPI (green), Pydantic v2 (blue), SQLAlchemy 2.0 (gray/blue), TensorFlow (orange)
- Visual elements: TensorFlow neural network icon, asyncio logo, 5-10ms ML inference badge
- gRPC components: grpcio server, async generators, async queues
- Data flow: Blue gradient arrows connecting to Redis and PostgreSQL
- Performance metrics badge: "18,000 req/sec | 15ms P99 latency | 5ms ML inference"

CENTER BRIDGE SECTION (between columns):
- Large Protocol Buffers logo with hex blocks: "08 65 12 05 41 6C 69 63 65"
- Label: "UNIFIED PROTOCOL BUFFER CONTRACT" in bold
- Two-way arrows connecting all three platforms showing shared contract
- OpenTelemetry waveform connecting all three columns

BOTTOM SECTION - Shared Infrastructure:
- Redis Cluster logo (red) with "State + Pub/Sub" label
- PostgreSQL logo (blue elephant) with "Persistent Storage" label
- Kubernetes logo (blue) with "Service Mesh" label
- Envoy proxy logo (purple) with "gRPC Load Balancer" label
- OpenTelemetry collector with Jaeger, Prometheus, Grafana logos
- Horizontal Pod Autoscaling (HPA) visualization with scaling arrows

BOTTOM RIGHT - Observability Panel:
- Distributed trace visualization showing trace ID spanning all three platforms
- Jaeger logo, Prometheus logo, Grafana logo
- Metrics graphs showing latency and throughput across platforms

Color palette: Clean white background (#FFFFFF), subtle grid pattern, .NET purple (#512BD4), Node.js green (#68A063), Python blue (#3776AB), gRPC blue (#00B5E2), teal accents (#00ACC1). Soft shadows, rounded rectangles, modern technical illustration style.

Text "Vineet Sharma" positioned at bottom right corner in clean sans-serif font, 12pt, medium gray (#6C757D), with small multi-platform icons (dotnet, nodejs, python) next to signature.

16:9 landscape orientation, ultra-high resolution (3840×2160), suitable for conference presentations, technical documentation covers, and blog post headers.

```

## Prompt .NET 10: CLR, CTS, CLS, JIT, and GC - The Silent Guardians Architectural Deep Dive 

```
Create a bright, professional Microsoft-style technical infographic in landscape format (16:9) for a .NET 10 architecture article. Use a subtle Microsoft gradient background (light blue to white, #E5F1FB to #FFFFFF) with subtle grid lines typical of Microsoft documentation style.

The graphic must feature five distinct technology sections arranged in a workflow pipeline from left to right across the middle of the image.

TOP SECTION (Large, prominent):
- Title: ".NET 10: CLR, CTS, CLS, JIT, and GC - The Silent Guardians Architectural Deep Dive" in bold Microsoft blue (#0078D4), 28pt
- Subtitle: "CLR, CTS, CLS, JIT, and Garbage Collection: .NET 10 Code Runs 45% Faster Than .NET 8 — And What Changed Under the Hood" in dark gray (#505050), 16pt
- Small .NET logo (purple square with dot) at top-left corner

MIDDLE SECTION (Five connected workflow boxes, smaller size but more detailed content):

BOX 1 - CLR (Common Language Runtime) - Microsoft Blue theme:
┌─────────────────────────────────────┐
│           ⚙️ CLR ⚙️                  │
│      Common Language Runtime        │
├─────────────────────────────────────┤
│ • Memory Management (GC)            │
│ • Exception Handling                │
│ • Security & CAS                    │
│ • Assembly Loading                  │
│ • Thread Management                 │
│ • Pinned Object Heap (NEW)          │
└─────────────────────────────────────┘

BOX 2 - CTS (Common Type System) - Microsoft Green theme:
┌─────────────────────────────────────┐
│           📊 CTS 📊                  │
│       Common Type System            │
├─────────────────────────────────────┤
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌───────┐  │
│ │ int │ │long│ │Half│ │Int128 │  │
│ └─────┘ └─────┘ └─────┘ └───────┘  │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌───────┐  │
│ │string│class│struct│Vector512│  │
│ └─────┘ └─────┘ └─────┘ └───────┘  │
│ • Value Types vs Reference Types   │
│ • Boxing/Unboxing (0 alloc in .NET10)│
│ • Generic Math INumber<T>          │
└─────────────────────────────────────┘

BOX 3 - CLS (Common Language Specification) - Microsoft Purple theme:
┌─────────────────────────────────────┐
│           📜 CLS 📜                  │
│     Common Language Specification   │
├─────────────────────────────────────┤
│ ┌─────┐ ┌─────┐ ┌─────┐            │
│ │ C#  │ │ VB  │ │ F#  │            │
│ └─────┘ └─────┘ └─────┘            │
│ • Rules for language interop        │
│ • No unsigned types in public APIs  │
│ • No pointers in contracts          │
│ • Generic variance (out T/in T)     │
│ • Case-insensitive safe (VB.NET)    │
│ • Default interface methods         │
└─────────────────────────────────────┘

BOX 4 - JIT (Just-In-Time Compiler) - Microsoft Orange theme:
┌─────────────────────────────────────┐
│           🚀 JIT 🚀                  │
│       Just-In-Time Compiler         │
├─────────────────────────────────────┤
│ Tier 0: Quick JIT (~1ms)            │
│    ↓ (30+ calls - PGO)              │
│ Tier 1: Optimizing JIT (~20-100ms)  │
├─────────────────────────────────────┤
│ • AVX-512 auto-vectorization        │
│ • ML-guided inlining (depth 25)     │
│ • Loop unrolling (2x-8x predicted)  │
│ • Guarded devirtualization (GDV)    │
│ • Range check elimination (95%)     │
│ • Hot/Cold method splitting         │
└─────────────────────────────────────┘

BOX 5 - GC (Garbage Collection) - Microsoft Red/Orange theme:
┌─────────────────────────────────────┐
│           🗑️ GC 🗑️                  │
│        Garbage Collection           │
├─────────────────────────────────────┤
│ ┌──────┐ ┌──────┐ ┌──────┐         │
│ │ Gen0 │→│ Gen1 │→│ Gen2 │         │
│ │ephemeral│survivor│long-lived│    │
│ └──────┘ └──────┘ └──────┘         │
│                                     │
│ ✅ Pinned Object Heap (NEW .NET10)  │
│ ✅ LOH Background Compaction        │
│ ✅ Server GC (1 heap per CPU)       │
│ ✅ Background GC (<1ms pauses)      │
│ ✅ TryStartNoGCRegion (real-time)   │
└─────────────────────────────────────┘

CONNECTORS: Bright blue curved arrows (#0078D4) with 3px thickness connecting CLR → CTS → CLS → JIT → GC in a flowing pipeline.

BOTTOM SECTION (Three panels side by side):

PANEL 1 (Left) - Code Comparison:
┌─────────────────────────────────────────────────┐
│  📝 .NET 1.0 / .NET 8 Code                      │
│  ┌───────────────────────────────────────────┐  │
│  │ int sum = 0;                               │  │
│  │ for(int i = 0; i < arr.Length; i++)        │  │
│  │ {                                           │  │
│  │     sum += arr[i];  // scalar, 1 int/cycle │  │
│  │ }                                           │  │
│  │ return sum;                                │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  🚀 .NET 10 Code                                │
│  ┌───────────────────────────────────────────┐  │
│  │ Span<int> span = arr;                      │  │
│  │ Vector512<int> sumVec = Vector512<int>.Zero│  │
│  │ for(int i=0;i<span.Length;i+=16)           │  │
│  │ {                                           │  │
│  │     var v = Vector512.Create(span, i);     │  │
│  │     sumVec = Avx512F.Add(sumVec, v);       │  │
│  │ }            // 16 ints per cycle!         │  │
│  │ return Vector512.Sum(sumVec);              │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘

PANEL 2 (Center) - Performance Matrix:
┌─────────────────────────────────────────────────┐
│  📊 PERFORMANCE MATRIX (.NET 8 → .NET 10)       │
├────────────────────┬────────────┬───────────────┤
│  Metric            │ .NET 8     │ .NET 10       │
├────────────────────┼────────────┼───────────────┤
│  📐 Math Ops/ms    │ 52M        │ 84M    ▲ 61%  │
│  📊 Array Sum/ms   │ 122M       │ 194M   ▲ 59%  │
│  🗑️ GC Pause (typ) │ 5.0ms      │ 0.5ms  ▲ 90%  │
│  🚀 Startup (cold) │ 8.0ms      │ 2.0ms  ▲ 75%  │
│  🔗 P/Invoke calls │ 2.8M/ms    │ 6.6M/ms▲135%  │
│  💾 Memory (base)  │ 60MB       │ 35MB   ▲ 42%  │
└────────────────────┴────────────┴───────────────┘

PANEL 3 (Right) - Key Improvements Summary:
┌─────────────────────────────────────────────────┐
│  ✨ KEY IMPROVEMENTS IN .NET 10                 │
├─────────────────────────────────────────────────┤
│  ✅ Pinned Object Heap (Zero fragmentation)     │
│  ✅ Int128 / UInt128 hardware acceleration      │
│  ✅ AVX-512 auto-vectorization (16-wide)        │
│  ✅ Native AOT (Production ready)               │
│  ✅ Tiered PGO (Default in .NET 10)             │
│  ✅ ML-guided inlining & loop unrolling         │
│  ✅ Generic variance (Full out T/in T)          │
│  ✅ LOH Background Compaction                   │
│  ✅ Half precision (16-bit float) acceleration  │
│  ✅ Collection expressions (C# 12)              │
└─────────────────────────────────────────────────┘

FOOTER SECTION:
Dark gray bar (#2D2D2D) at bottom with white text:
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  📘 Medium    🔗 LinkedIn                                                           │
│  Vineet Sharma | medium.com/@mvineetsharma | linkedin.com/in/vineet-sharma-architect│
└─────────────────────────────────────────────────────────────────────────────────────┘

Style: Microsoft Fluent UI, flat design, crisp 1px borders, rounded corners (8px), ample white space, extremely readable, 16:9 aspect ratio. Use subtle Microsoft gradient background (top to bottom: #E5F1FB to #FFFFFF). Add subtle grid dots (like graph paper) in background at 10% opacity. No harsh shadows. Professional Microsoft Learn documentation aesthetic with vibrant accent colors.
```

## iChatClient 

```
Create a light, flashy, vibrant technical illustration for a Medium article titled "One Interface to Rule Them All: How Microsoft.Extensions.AI Changed the Way .NET Talks to LLMs".

BACKGROUND: White/silver gradient (#FFFFFF to #F0F2F5) with subtle semi-transparent human brain silhouette made of neural network nodes at low opacity. Brain has glowing synapse connections in electric blue (#0088FF) and neon cyan (#00D4FF). Add diagonal grid lines in light gray (#E5E7EB), floating data particles in blue, pink, purple, green. Soft glowing orbs: cyan bottom-left, purple top-right, pink center-right.

TOP HEADER: Main Title "ONE INTERFACE TO RULE THEM ALL" in bold gradient #5C2D91 to #B400FF to #FF2D75 centered. Subtitle "How Microsoft.Extensions.AI Changed the Way .NET Talks to LLMs" in #1F2937 centered italic. Tagline "Stop rewriting your AI integration every time you switch from OpenAI to Azure to Ollama. Here's the .NET 10 abstraction that finally gets it right — with caching, telemetry, and function calling built in." in #4B5563 centered. Tech Badges "Microsoft.Extensions.AI", "IChatClient", ".NET 10", "Open Source" as rounded pills #F3F4F6 with #4B5563 text.

CENTRAL HUB: Large glowing .NET badge gradient #5C2D91 to #B400FF with text "IChatClient" in white bold. Radiating connector lines in blue #0088FF, cyan #00D4FF, pink #FF2D75, purple #B400FF, green #00E882 with directional arrows and flowing data particles.

LEFT SIDE - MIDDLEWARE PIPELINE: Vertical pipeline with 6 white cards connected by neon green #00E882 downward arrows.

Card 1: Icon document+magnifying glass #0088FF, title "UseLogging()", description "ILogger integration | Request/Response logging | Token usage tracking"
Card 2: Icon database+lightning #00E882, title "UseDistributedCache()", description "IDistributedCache | Redis, SQL Server, Memory | 40% cost reduction"
Card 3: Icon line chart+brackets #B400FF, title "UseOpenTelemetry()", description "GenAI Semantic Conventions v1.39 | Traces & Metrics | Latency tracking"
Card 4: Icon two gears+sparkle #FF2D75, title "UseFunctionInvocation()", description "Automatic tool calling | .NET methods as AI tools | Approval workflows"
Card 5: Icon scissors cutting text #F97316, title "UseChatReducer()", description "Context window management | Auto-truncation | Token limit safety"
Card 6: Icon speedometer+checkmark #00D4FF, title "Custom middleware", description "Custom implementation | Burst handling | Quota management"

Each card: white background, border-radius, shadow, colored border, padding.

RIGHT SIDE - LLM PROVIDER CARDS: 2 columns x 4 rows = 8 white cards.

Card 1 OpenAI: Green #10A37F logo white circle, title "OpenAI", model "GPT-4o / GPT-4 Turbo", specs "📊 128K tokens | 📤 16K tokens | ⚡ Token streaming | 🔧 Native functions | 👁️ Multimodal vision | 💰 $2.50/1M input", status "● LIVE"

Card 2 Azure OpenAI: Blue #0072C6 logo Azure "A", title "Azure OpenAI", model "GPT-4o / GPT-4 / GPT-3.5", specs "📊 128K tokens | 📤 16K tokens | ⚡ SSE streaming | 🔧 Native functions | 👁️ Multimodal | 🔒 RBAC, VNet, PII", status "● LIVE"

Card 3 Google Gemini: Blue #4285F4 logo star/sparkle, title "Google Gemini", model "Gemini 1.5 Pro / 2.0 Flash", specs "📊 2M tokens largest | 📤 8K tokens | ⚡ Token streaming | 🔧 100+ tools | 👁️ Video+Audio+Images | 🌐 Google Search grounding", status "● LIVE"

Card 4 Anthropic: Orange #D97757 logo stylized "C" with leaf, title "Anthropic", model "Claude 3.5 Sonnet / 3 Opus", specs "📊 200K tokens | 📤 4K tokens | ⚡ Token streaming | 🔧 Beta functions | 👁️ Images | 🛡️ Constitutional AI", status "● LIVE (Functions: ● BETA)"

Card 5 DeepSeek: Deep Blue #0D3B66 logo abstract "D" with circuit, title "DeepSeek", model "DeepSeek-V3 / R1", specs "📊 128K tokens | 📤 32K tokens R1 | ⚡ Token streaming | 🔧 Native functions | 🧮 Chain-of-thought reasoning | 💰 $0.14/1M input cheapest", status "● LIVE"

Card 6 Ollama: Purple #7C3AED logo llama silhouette, title "Ollama", model "Llama 3.2 / Mistral / Phi", specs "📊 Configurable 4K-32K | 📤 Configurable | ⚡ Token streaming | 🔧 Experimental functions | 💾 MMap/MLock memory | 🆓 $0 local free", status "● LIVE"

Card 7 ONNX Runtime: Orange #F97316 logo orange cube, title "ONNX Runtime", model "Phi-3 / Mini / Small", specs "📊 4K-128K dependent | 📤 2K-8K | ⚡ Native streaming | 🔧 Not yet | 🔒 Air-gapped no API | ⚙️ CPU/CUDA/DirectML", status "● LIVE"

Card 8 Custom Provider: Rainbow gradient logo puzzle piece + code brackets, title "Custom Provider", model "Your Implementation", specs "📊 Your choice | 📤 Configurable | ⚡ IAsyncEnumerable | 🔧 Implement IChatClient | 🎨 Full control | 🔌 GetService<T> escape hatch", status "⚡ READY"

Each card: white background, border-radius, shadow, padding.

BOTTOM CENTER - CODE SNIPPET: Modern code editor window with window controls red/yellow/green dots top left, file tab "CustomLoggingChatClient.cs", dark mode background #1E1E2E. Code:

public class CustomLoggingChatClient : DelegatingChatClient
{
private readonly ILogger _logger;

public CustomLoggingChatClient(IChatClient inner, ILogger<CustomLoggingChatClient> logger)
: base(inner) => _logger = logger;

public override async Task<ChatResponse> GetResponseAsync(
IEnumerable<ChatMessage> messages,
ChatOptions? options = null,
CancellationToken ct = default)
{
_logger.LogInformation("📤 Request: {Count} messages", messages.Count());
var stopwatch = Stopwatch.StartNew();
var response = await base.GetResponseAsync(messages, options, ct);
stopwatch.Stop();
_logger.LogInformation("📥 Response: {Elapsed}ms", stopwatch.ElapsedMilliseconds);
return response;
}
}

Syntax highlighting: keywords #B400FF purple, strings #F97316 orange, classes #00D4FF cyan, comments #6B7280 gray, methods #0088FF blue, variables #10A37F green.

BOTTOM FOOTER BAR: Semi-transparent white #FFFFFF at high opacity, rounded corners at bottom, soft shadow.

LEFT SECTION Badges: "📖 MIT License" purple border, "⭐ Star on GitHub" orange border, "🚀 .NET Foundation", "🤝 Contribute"

CENTER SECTION: "💡 Write once, deploy anywhere" italic #6B7280

RIGHT SECTION Social Attribution (CRITICAL - MUST INCLUDE EXACTLY):

Vineet Sharma (bold #1F2937)

mvineetsharma.medium.com with Medium logo (green circle #00AB6C with white M) - link color #0088FF

linkedin.com/in/vineet-sharma-architect with LinkedIn logo (blue circle #0A66C2 with white in) - link color #0A66C2

Use actual Medium logo (green circle with white M) and LinkedIn logo (blue circle with white in). No text icons. No size specifications. No "20px" or similar measurements.

VISUAL ENHANCEMENTS: Dashed animated arrows between middleware cards #00E882, glowing nodes at connection points, data particles flowing on connector lines, status dots green ● LIVE and yellow ● BETA, brain background with neuron firing sparkles, .NET sparkle stars #512BD4, connector labels "IChatClient", "Middleware Pipeline", "Provider Adapters".

COLOR PALETTE: Background #FFFFFF to #F0F2F5, Brain overlay rgba(0,136,255,0.08), .NET Hub #5C2D91 to #B400FF, Connectors #0088FF #00D4FF #FF2D75 #B400FF #00E882, Cards #FFFFFF, Code window #1E1E2E, Text primary #1F2937, Text secondary #4B5563, Success #00E882, Warning #F97316, LinkedIn #0A66C2, Medium #00AB6C.

RESOLUTION: 16:9 landscape, high resolution, modern tech blog hero image style, Stripe/Datadog aesthetic, light mode, flashy, professional, developer-focused, AI-themed with brain background. Crisp text rendering using system fonts.

VERIFICATION CHECKLIST: Main title present, subtitle and tagline present, tech badges present, brain background visible, 6 middleware cards with arrows, 8 LLM provider cards with specs, code snippet at bottom, Vineet Sharma name in footer, mvineetsharma.medium.com link with Medium logo, linkedin.com/in/vineet-sharma-architect link with LinkedIn logo, connector lines with arrows, status indicators on LLM cards, neon accents throughout, no text cutoff, no size specifications like 20px.
```

## Encryption - Decryption Explained using .NET 10
```
Create a detailed, modern infographic-style technical illustration showcasing .NET 10 Encryption & Decryption architecture in an open-source style theme with a LIGHT BACKGROUND (soft white/off-white with subtle gray gradients). Add light cybersecurity pattern overlays (faint padlocks, shields, binary code, network nodes in light gray/silver).

Main Title at top in bold dark blue/purple: "Encryption/Decryption Explained using .NET"

Subtitle directly below in smaller text: "Modern Encryption Patterns in .NET 10: AES-GCM, PQC, ZKP & Hardware Acceleration"

Central Graphics-Focused Architecture (emphasis on cryptographic visuals):

Left Side - Encryption Zone:
- Large glowing lock icon (closed, green/gold) with data flowing in
- AES (Advanced Encryption Standard) 3D gear/cog icon with "256-bit" badge
- RSA key pair icons (two interlocking keys - one gold public, one silver private)
- ChaCha20 stream cipher wave animation icon
- Label: "ENCRYPTION" in bold with arrow pointing right

Middle - Crypto Processor Hub (most prominent visual):
- Central glowing hexagon processor chip with .NET logo in center
- Surrounding circular icons connected by glowing lines:
  - SHA-256 hash function (fingerprint/fingerprint icon)
  - GCM/GMAC badge (checkmark inside shield)
  - Post-Quantum Cryptography (quantum atom + shield combined icon)
  - Zero-Knowledge Proof (eye with slash + checkmark icon)
  - Hardware Acceleration (microchip with lightning bolt)
- Colorful node connections forming a network web
- Small rotating key rings orbiting the central chip

Right Side - Decryption Zone:
- Large open lock icon (green, unlocked) with verified data flowing out
- Authentication tag badge (green checkmark)
- Integrity verified stamp graphic
- Label: "DECRYPTION" with arrow pointing from center

Add 3 Floating Code Snippets as Sticky Notes/Cards:
Card 1 (top left corner): 
`using System.Security.Cryptography;`
`using var aes = Aes.Create();`

Card 2 (middle right edge):
`byte[] encrypted = aesGcm.Encrypt(`
`    plaintext, nonce, aad);`

Card 3 (bottom left corner):
`byte[] decrypted = aesGcm.Decrypt(`
`    ciphertext, tag, aad);`

Workflow Connectors (bold, colorful arrows):
- Solid teal/cyan arrows for ENCRYPTION FLOW (left to center)
- Dashed orange/coral arrows for DECRYPTION FLOW (center to right)
- Purple dotted lines for KEY EXCHANGE (orbiting around center)
- Red circular security checkpoints with small shield icons at connection points

Background Elements (light and subtle):
- Faint grid of encrypted binary data (010101) in light gray
- Ghosted watermark-style lock and shield icons
- Light network node connection lines (like constellation)
- Delicate circuit board traces in silver/champagne
- Cyber security hexagon patterns as transparent overlays
- Holographic-style key rings (semi-transparent)

Add a Subtle Venn Diagram (bottom middle or top right corner) showing:
- Circle 1: Confidentiality (lock icon)
- Circle 2: Integrity (shield icon)
- Circle 3: Availability (server icon)
- Circle 4: Authentication (fingerprint icon)
- Overlapping area labeled "SECURE DESIGN"

Bottom Section - Professional Attribution (removed "Article by"):
Left side: "✍️ Vineet Sharma" with pen icon
Center: "📘 Medium: mvineeetsharma.medium.com" with Medium (M) square logo icon (green/white background)
Right side: "💼 LinkedIn: linkedin.com/in/vineet-sharma-architect" with LinkedIn (in) circular logo icon (blue/white background)

Add small social/professional icons before each profile reference.

Style Requirements:
- OPEN-SOURCE COLOR PALETTE: Soft white background, Navy blue (#1a2a4f), Teal (#20b2aa), Purple (#7b2d8e), Coral (#ff6b6b), Gold (#ffd700)
- FLAT DESIGN with SOFT GRADIENTS and light shadows
- GLOWING NODES in neon teal and purple (subtle enough for light background)
- GLASSMORPHISM panels with frosted glass effect (white with opacity)
- 3D DEPTH on cryptographic components (keys, locks, chips)
- TECHNICAL DOCUMENTATION aesthetic (clean, readable, organized)
- CRISP, READABLE LABELS in monospace (code) and sans-serif (headings) fonts
- HIGH RESOLUTION (4K suitable for printing and online publication)
- MODERN GRAPHIC DESIGN with emphasis on cryptography visuals (locks, keys, shields, chips, network nodes)
```