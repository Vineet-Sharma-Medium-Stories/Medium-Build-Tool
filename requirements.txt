# Standard library components do not need to be listed here.
# Optional: for clipboard support if we add it later
pyperclip==1.8.2
# Optional: if you want to perform advanced markdown analysis
markdown==3.4.1

google-generativeai>=0.8.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# pip install google-genai Pillow python-dotenv
google-genai>=0.2.0


cairosvg>=2.7.0  # Optional - for PNG conversion